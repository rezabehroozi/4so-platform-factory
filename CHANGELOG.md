# 0.0.219 — AI-native operator experience program rebaseline

- Rebased the canonical roadmap to `PROGRAM_PHASE_MODEL_V3` while retaining eight large delivery phases and adding seven cross-cutting product tracks: Operator Experience/UI/UX, AI-Native, MCP/External Agents, Lab/Certification, Security/RBAC/Evidence, Supply Chain/Disconnected and Developer/Agent Experience.
- Promoted the TailAdmin-informed console work into the product-owned **4SO Operator Horizon V1** theme; CoreUI remains an accessibility reference and no template framework/PRO asset is introduced as a runtime dependency.
- Bound all later phases to complete console workflows, AI/MCP read surfaces where appropriate, documentation, evidence and deterministic tests rather than allowing backend-only closure.
- Expanded final certification to include responsive/RTL/accessibility/performance, prompt-injection/secret-leakage/provider-outage/cross-tenant AI evals and MCP authorization/interoperability negative controls alongside M00-M13 physical runtime evidence.

# 0.0.218 — AI-native Operator Console design foundation

- adopt TailAdmin Community (MIT) as the external visual/layout reference after a CoreUI/AdminMart/TailAdmin fit audit, while retaining the embedded 4SO HTML/CSS/JavaScript runtime and all 4SO information architecture, authority and workflow semantics; no React/Next/MUI/Bootstrap/Tailwind/Alpine runtime migration is introduced solely for appearance.
- establish a 4SO-specific enterprise shell with high-contrast navigation, sticky header, global navigation-only `Ctrl/Cmd+K` command search, explicit persisted light/dark mode, denser operational cards/tables/action surfaces and responsive behavior. Template demo business domains and decorative analytics are not copied.
- close a real Operator Console regression hidden by previous coverage: the AI page used a missing `fillSelect()` helper and could stop rendering durable run history/usage/MCP state after successful API responses; the AI/Lab surfaces are now part of the headless release navigation contract.
- close 320px Lab horizontal overflow caused by long authority identifiers and harden mobile identifier wrapping without making data tables silently non-scrollable.
- harden the UI Release Gate so each viewport owns an isolated Python/Playwright/Chromium lifecycle and dirty-form semantics no longer depend on a presentation-only page title being visible at every responsive breakpoint.
- record the design decision and licensing boundary in `docs/OPERATOR_CONSOLE_DESIGN_FOUNDATION.md`; TailAdmin PRO assets are excluded and direct copied Community source, if ever introduced, must retain the MIT notice.
- preserve the AI-native Phase-C and four-layer Release Gate boundaries from 0.0.217; this UI/source closure is not Exact-SHA Physical Runtime evidence.

# 0.0.217 — AI-native control-plane foundation

- establish `UNIFIED_AI_RUNTIME_V1` as the single provider-neutral AI execution boundary for Operator diagnosis, Marketplace advisory and cloud/local Lab diagnosis. OpenAI Responses and OpenAI-compatible providers share one fail-closed configuration, timeout/input/output budget, mandatory pre-egress redaction and structured-output path; deterministic platform workflows remain available when AI is disabled.
- add durable project-scoped `ai_runs` authority with rolling-safe migration `0053`. Successful advisory calls persist provider/model, prompt/context/request/output digests, redaction count and token usage without persisting raw prompts or credentials. Canonical JSON output digests are recomputed on write/read/restore so PostgreSQL `jsonb` formatting cannot create false drift and tampered output cannot be accepted as durable evidence.
- close duplicate-token spending on idempotent Marketplace/Operator retries: replay is resolved from durable AI-run/request authority before a second provider call is admitted. Linked operation/managed-cluster evidence is revalidated against the same project in Memory/File/PostgreSQL.
- add dedicated API-token capability scopes `mcp.read` and `ai.diagnose`. MCP remains stateless/read-only on protocol `2026-07-28`; project-scoped tools re-apply authoritative project access instead of trusting only the outer HTTP boundary. The lab guide/runtime MCP tool lists are contract-tested to prevent UI/external-agent capability drift.
- upgrade the Operator Console AI Control Plane from a thin diagnosis form to an operational surface backed only by live authority: effective provider/model/budgets, explicit advisory/PASS boundaries, aggregate token/cache/redaction usage, MCP endpoint/protocol/scopes/tools, project-filtered durable run history and inspectable structured evidence. AI history is live-refreshed and provider health is never inferred merely from configuration.
- keep Lab AI failure-only and low-token: the deterministic Lab runner owns preflight/install/matrix/evidence/PASS, while AI receives bounded redacted failure packets and can only diagnose or propose checks. Codex/Claude/Antigravity remain optional repair/diagnosis workers rather than certification authorities.
- expand Phase C to `C-ai-native-control-plane-lab-mcp-foundation` while preserving the eight large program phases. Remaining Phase-C blockers are real server-driven M00-M03 execution, immutable automatic open-source acquisition, external provider/PostgreSQL durability/MCP interoperability runtime certification and a physical failure-to-AI-diagnosis run; no source/local test is promoted to Exact-SHA Physical PASS.

# 0.0.216 — Lab certification, AI diagnosis and MCP foundation

- rebaseline `PROGRAM_PHASE_MODEL_V2` into eight large delivery phases instead of many narrow increments: A) architecture rebaseline, B) target capability-contract foundation, C) Lab Certification + AI + MCP Foundation, D) OKD import runtime certification, E) OKD capability/profile/catalog integration, F) imported Day-2 + managed Compact-3, G) disconnected + upgrade/recovery, and H) full Exact-SHA physical certification with chaos/soak/multi-cluster evidence. Phase C is current and remains `blocked` until its physical/runtime exit criteria are evidenced.
- add `LAB_CERTIFICATION_MATRIX_V1` as the machine-readable lab authority with four server tiers and M00-M13 certification rows. Each row declares phase ownership, automation status (`IMPLEMENTED`, `PARTIAL`, `PENDING`, or `PENDING_PHASE`), destructive scope, actions and AI eligibility so roadmap presence is never represented as executable coverage.
- add `scripts/lab_runner.py` and `schemas/lab-execution.schema.json` as the deterministic server-driven lab foundation. The runner validates the exact release artifact/server inventory, performs fail-closed SSH/resource preflight and plans product-owned installation without allowing an AI agent to own PASS/FAIL or the happy path. Missing immutable dependency authority remains `BLOCKED`; the runner does not fetch arbitrary `latest` upstream software.
- add a failure-only bounded AI diagnosis contract with OpenAI-compatible provider support and optional Codex/Claude/Antigravity worker identities. AI receives normalized bounded failure packets only and cannot declare deterministic, runtime-certification or Physical PASS.
- add read-only MCP exposure through `POST /mcp` plus `GET /api/v1/lab/guide`, using the modern 2026-07-28 protocol shape for external clients. MCP currently exposes lab-guide and target-architecture authorities only; no mutating MCP tool is admitted.
- add the Operator Console Lab & Certification view backed directly by the same lab authority, including required server counts/roles, matrix actions, destructive flags, automation truth and AI/MCP policy. No separate hand-maintained UI roadmap is introduced.
- preserve Release Gate truth: source/local closure of the new foundation is not server-driven lab certification, external MCP interoperability, AI runtime diagnosis proof or Exact-SHA Physical Runtime evidence.

# 0.0.215 — Cluster inventory observation epoch authority closure

- close a Phase-C mutation-authority defect where inventory freshness was derived only from Hub receipt time: an authenticated Agent could submit an old observation now, refresh `LastSeenAt` / `InventoryUpdatedAt`, and make stale target capability/identity evidence appear fresh enough for a new task claim.
- persist a distinct `inventoryObservedAt` authority epoch across Memory/File/PostgreSQL, reject zero, stale, excessively future-skewed and backwards observation timestamps at the Store boundary, and require both fresh Hub receipt and fresh target observation for new mutation task claims. Equal observation timestamps remain idempotently reportable but cannot extend authority beyond the original target-observation age.
- add rolling-safe migration `0052` as a nullable/default-free observation epoch. Existing rows intentionally remain NULL and fail closed for new mutation task claims until a fresh inventory report arrives; migration `0050` and `0051` remain byte-for-byte unchanged.
- preserve leased-task completion semantics: claim-time freshness gates new work, while an already-fenced/leased task is still judged by its unchanged authority epoch plus lease/fence so a legitimate long-running task is not invalidated merely because the three-minute claim window expires during execution.
- add Memory/PostgreSQL negative controls for stale/future/rollback observation epochs and update deterministic fixtures so stale-health behavior is created by time advancing after a valid observation rather than by accepting stale evidence at ingestion. Phase C remains blocked on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; this source/generated-runtime closure is not Exact-SHA Physical Runtime proof.

# 0.0.214 — Cluster import PostgreSQL expiry authority closure

- close a production-backend parity defect where PostgreSQL left expired `PENDING_APPROVAL` / `APPROVED` cluster-import rows logically active: reads could report stale enrollment state and the partial unique name index could prevent a replacement import even after the credential expiry time had passed.
- make PostgreSQL reject already-expired enrollment creation, render effective expiry on `Get/List`, reject revoke after logical expiry, and atomically materialize an expired same-name generation as `EXPIRED` with credential invalidation plus `cluster_import.expired` audit/outbox evidence before creating its replacement.
- preserve Memory/File semantics and Phase C truth: this is a runtime correctness closure for the PostgreSQL authority backend; `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING` remains open and no Exact-SHA Physical Runtime PASS is inferred.
- harden the headless UI release gate by restarting Chromium between the Product Console and Installer viewport matrices; this preserves all 10 viewport checks while preventing cumulative renderer/process stalls from turning an otherwise passing gate into an unbounded timeout.

# 0.0.213 — Target RBAC upgrade read-only parity closure

- preserve migration `0050` byte-for-byte and add rolling-safe migration `0051` instead of rewriting applied history; this closes a PostgreSQL/FileStore upgrade-state parity gap where `0050` removed unsafe legacy successor mutation authority but PostgreSQL did not expose the same explicit `target-read-only-admission` capability truth as FileStore.
- scope the parity repair to already-inventoried successor generations that have neither active nor issued mutation RBAC and that predate (or still await) their revoked predecessor acknowledgement, so a genuinely new same-UID re-enrollment created after acknowledgement is not prematurely marked admitted before its first authoritative inventory.
- add migration-contract regression coverage for the narrow successor guards and explicit rolling-safe compatibility classification; Phase C remains blocked on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`, and this source/upgrade repair is not Exact-SHA Physical Runtime proof.

# 0.0.212 — Target RBAC revocation acknowledgement/history upgrade closure

- enforce generation ordering for physical-cluster re-enrollment: after Hub revocation, a new ClusterImport claiming the same `kube-system` UID is rejected until the revoked predecessor's target-side RBAC fence is acknowledged. The acknowledgement must carry the exact canonical `targetRBACRevocationFenceDigest`, is revisioned/audited/outboxed across Memory/File/PostgreSQL, and is explicitly operator acknowledgement rather than Physical Runtime proof.
- add migration `0050` as `QUIESCED_REQUIRED`. It backfills sticky `target-mutation-rbac-ever-issued` history from immutable `cluster.mutation_rbac_activation.authorized` audit events and repairs legacy same-UID successors that earlier versions allowed before predecessor fencing by removing current mutation issuance/active authority and forcing them read-only until acknowledgement. FileStore snapshot restore performs the equivalent audit backfill and legacy successor repair.
- close revoke-after-drift history loss: server-owned `target-mutation-rbac-ever-issued` survives current authority invalidation, so a revoke still emits the full target-side mutation RBAC fence whenever activation was ever issued/proven. Issued-but-not-yet-proven activation is conservatively treated as potentially applied.
- keep mutation-RBAC authorization basis scoped to stable target identity/distribution authority. Unrelated OLM/API/CRD/OpenAPI discovery churn changes the full authenticated inventory digest used by planning/task safety but does not revoke Kubernetes RBAC; Kubernetes/distribution/identity authority drift still fails closed.
- extend Fleet generated-runtime coverage to `revoke -> blocked same-UID re-enrollment -> exact-digest acknowledgement -> re-enrollment with a new import-scoped principal`. Phase C remains blocked on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; acknowledgement is not target-side apply evidence and does not constitute Exact-SHA Physical Runtime PASS.

# 0.0.211 — Mutation RBAC discovery-stability and revocation-history closure

- remove unrelated API/CRD/OpenAPI discovery catalog churn from the mutation-RBAC authorization basis while retaining it in the full inventory digest used by planning and task compatibility. This prevents ordinary OLM/operator installation from needlessly revoking platform mutation RBAC.
- add sticky server-owned `target-mutation-rbac-ever-issued` history so later authority drift cannot erase evidence that mutation RoleBindings may remain on the target. Revocation uses this durable history to fence all target-side mutation bindings even when current active/issued authority has already been invalidated.
- make full target-side revocation fencing idempotent for issued-but-never-proven activation by ensuring managed namespaces before empty-subject RoleBindings, while read-only targets that never received mutation issuance retain the minimal two-binding fence.
- keep Phase C blocked on real OKD runtime certification; no source/generated-runtime result is promoted to Physical PASS.

# 0.0.210 — Mutation RBAC safe-HTTP and stable-authority-basis closure

- make mutation RBAC issuance an explicit state-changing API operation. `POST /api/v1/clusters/{id}/mutation-rbac-manifest` now issues/re-issues digest-bound authorization; `GET` is read-only and returns only an already-current issuance. A cache, prefetch, health probe or ordinary manifest lookup can no longer create authorization state, audit entries or outbox events as a GET side effect.
- narrow `mutationRbacBasisDigest` from the entire non-mutation inventory to stable authorization facts: distribution/evidence, Kubernetes version, canonical API/CRD surface, schema authority, physical-identity continuity and enrollment-principal isolation. Volatile node readiness, capacity, certificate lifetimes, add-on health, storage inventory, networking telemetry and observability capabilities no longer force RBAC re-issuance during an incident.
- retain fail-closed behavior for real authority drift: Kubernetes/distribution/API/CRD/schema or identity/principal changes still invalidate `mutationRbacIssuedForDigest` and `target-mutation-rbac-active` until an explicit POST re-issues activation for the new basis. API/CRD lists are canonicalized before hashing so harmless discovery ordering does not churn authorization.
- keep task-level safety separate from RBAC authority. Full authenticated inventory digests continue to bind plans/tasks/revalidation, so telemetry or add-on drift can force plan revalidation without unnecessarily revoking the stable Kubernetes authorization principal.
- make cluster-detail truth explicit with `mutationRBACActivationIssued` and `mutationRBACProofPending` in addition to `mutationRBACActivationRequired`/`mutationEnabled`, so an issued-but-not-yet-proven activation is not represented as an unexplained disabled state.
- add source and generated-runtime regressions proving GET-before-issuance is side-effect free, POST issuance is idempotently retrievable, telemetry churn preserves current RBAC authority, task planning still revalidates on full inventory drift, and Kubernetes authority drift invalidates old issuance.
- keep Phase C blocked only on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; these source/generated-runtime corrections do not constitute real OKD target application or Exact-SHA Physical Runtime PASS.

# 0.0.209 — Mutation RBAC inventory-basis authority closure

- bind server-owned mutation RBAC activation to the exact non-mutation semantic inventory basis instead of retaining a boolean issuance marker across later topology/capability drift. `ManagedCluster` now persists both the current `mutationRbacBasisDigest` and `mutationRbacIssuedForDigest`; task admission requires a current issued marker whose issued-for digest exactly matches the current basis.
- compute the mutation basis from a normalized read-only inventory so applying mutation RBAC does not invalidate its own issuance, while any non-mutation semantic change such as Kubernetes/distribution/topology/capability drift changes the basis, strips `target-mutation-rbac-active` and server issuance, and returns the target to read-only until an explicit new activation is issued.
- add migration `0049` as a rolling-safe additive/defaulted authority for the two mutation-RBAC digest fields. Upgrades from pre-0049 state fail closed: a legacy `target-mutation-rbac-active` capability without digest-bound issuance cannot claim new tasks until a fresh inventory establishes the basis and the control plane re-issues activation.
- make the activation API render `issuedFromInventoryDigest` from the persisted issued-for basis rather than the post-activation inventory digest, and expose the current basis/issued-for digests in the response for operator evidence. Revocation fences reuse the issued-for digest when present.
- add Agent defense-in-depth: before any representative `SelfSubjectAccessReview`, the mutation activation ConfigMap must match the exact basis digest computed from the inventory the Agent is about to report, in addition to the existing managed-cluster ID, physical `kube-system` UID and revoked-marker checks.
- add MemoryStore, PostgreSQL and Agent negative controls proving semantic inventory drift invalidates old mutation issuance, re-issuance binds to the new basis, stale activation ConfigMaps fail before permission proof, and pre-0049 active state is read-only after upgrade.
- keep Phase C blocked only on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; this closes a source/generated-runtime authorization replay gap but is not real OKD import/reconnect/revocation evidence and does not constitute Exact-SHA Physical Runtime PASS.

# 0.0.208 — Target RBAC revocation fence and OKD restricted-runtime closure

- distinguish immediate Hub credential revocation from target-local Kubernetes authorization. ManagedCluster revoke now truthfully reports `hubAgentCredentialRevoked=true` and `targetRBACRevocationStatus=APPLY_REQUIRED` instead of implying that target RBAC disappears when the Hub credential is invalidated.
- return an idempotent target RBAC revocation fence with the revoke response and expose it again at `GET /api/v1/clusters/{id}/revocation-rbac-manifest` for revoked clusters. The fence marks the mutation activation proof revoked and clears all subjects from the enrollment read-only/credential bindings; mutation bindings are cleared only when the last authoritative inventory proved mutation RBAC active, so a never-activated read-only target does not fail revocation on namespaces that never existed.
- make `platform-agent` reject a mutation activation ConfigMap carrying `revoked: "true"` before UID or SelfSubjectAccessReview permission proof. Re-enrollment still uses a new import-scoped ServiceAccount generation; applying the new enrollment manifest re-binds only read-only access until a fresh mutation activation is admitted.
- make mutation RBAC issuance a server-owned authority instead of trusting an Agent capability claim. `target-mutation-rbac-activation-issued` is stripped from Agent inventory and can only be added by `AuthorizeClusterMutationRBACActivation`; `target-mutation-rbac-active` is ignored until that issuance exists, so a compromised/stale Agent cannot self-authorize mutation by reporting an active capability before the control plane has issued the activation manifest. Memory/File/PostgreSQL stores enforce the same two-stage invariant and smoke harnesses now exercise the real read-only → issuance → permission-proof sequence.
- make the fleet-agent Deployment explicit about a restricted runtime without pinning OKD-assigned UID/GID: pod `runAsNonRoot`, `RuntimeDefault` seccomp, container `allowPrivilegeEscalation: false`, read-only root filesystem and drop `ALL` capabilities. Writable credential state remains isolated to the existing `emptyDir`.
- add owner regressions for six-binding mutation-active revocation fencing, two-binding read-only fencing, missing inventory-digest normalization, revoked activation rejection before any permission proof, exact revoke-response truth, and post-revoke fence retrieval.
- make repository validation deterministic across developer workspaces and packaged artifacts by excluding `.pytest_cache` from the source-file inventory, matching the release builder exclusion set instead of allowing four local pytest cache files to inflate the validation count.
- keep Phase C blocked only on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; this source/generated-runtime closure is not target-side apply evidence and does not constitute Exact-SHA Physical Runtime PASS.

# 0.0.207 — Cluster re-enrollment principal isolation closure

- close physical-cluster re-enrollment parity across Memory/File/PostgreSQL. A `kube-system` UID may have only one non-revoked ManagedCluster authority at a time, while a fully revoked historical registration no longer permanently blocks an explicit re-enrollment of that same physical cluster. Memory snapshot restore independently rejects multiple active authorities for one physical UID.
- replace the global PostgreSQL `external_uid` uniqueness constraint with an active-authority partial unique index in migration `0047`. This migration is deliberately `quiesced-required`: old writers still use the legacy fixed agent ServiceAccount and must be stopped before post-revocation re-enrollment becomes possible.
- close a Kubernetes-authorization gap in revoke -> re-enroll. New ClusterImports persist a deterministic import-scoped agent ServiceAccount and enrollment/activation manifests bind read-only and mutation RBAC to that generation-specific principal, so a new enrollment cannot inherit mutation RoleBindings from a revoked generation. Existing imports keep the empty principal authority and therefore preserve the legacy `4so-platform-agent` ServiceAccount for rolling compatibility.
- make mutation activation consume the persisted ClusterImport principal authority rather than inferring it from an Agent capability. Modern isolated imports must both match the canonical import-scoped principal and attest `target-enrollment-principal-isolated`; legacy imports remain on the fixed principal until an explicit new enrollment. Agent inventory reports isolation only when the running ServiceAccount matches the principal derived from its import ID.
- add migration `0048` as rolling-safe additive authority for `cluster_imports.agent_service_account`, plus owner regressions proving concurrent duplicate physical authority is rejected, revoked history permits re-enrollment, the new enrollment receives a distinct principal, invalid PostgreSQL physical identity is rejected before mutation, and legacy/historical state remains readable.
- keep Phase C blocked only on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`. These source/database/RBAC corrections do not constitute real OKD import/reconnect/revocation evidence or Exact-SHA Physical Runtime certification.

# 0.0.206 — Cluster identity store authority closure

- move imported-cluster physical identity continuity into the authoritative Store boundary instead of relying on the HTTP handler as the only enforcement point. `UpsertClusterInventory` and `HeartbeatCluster` now receive the observed `kube-system` UID and Memory/File/PostgreSQL stores independently compare it with the UID bound at import claim.
- keep mixed-version behavior fail-closed at the owner layer: an inventory without `externalUid` remains observable but is normalized to read-only and cannot retain `target-cluster-uid-attested` or mutation RBAC authority; an identity-unattested heartbeat is authenticated but does not refresh connection liveness. A non-empty mismatched UID is rejected before inventory, digest, heartbeat or connection state can change.
- preserve the API-level conflict response as defense-in-depth while eliminating the possibility that a future in-process Store consumer bypasses the identity invariant. Add MemoryStore and scripted PostgreSQL owner regressions proving mismatch rejection and no liveness refresh for legacy heartbeats.
- keep Phase C truth unchanged: OKD is still preview/read-only and `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING` remains the sole phase blocker. No real OKD import/reconnect/revocation or Exact-SHA Physical Runtime PASS is inferred from this source closure.

# 0.0.205 — OKD/OpenShift identity continuity and activation binding closure

- close a distribution-authority correctness gap where any real `config.openshift.io/v1` `ClusterVersion/version` object was previously classified as `okd`. The Agent now distinguishes OKD release versions from Red Hat OpenShift/OCP, the target vocabulary keeps `openshift` as a separate recognized-but-not-admitted identity, and the control plane rejects OKD evidence whose release version is not actually an OKD build.
- add an independent server-side contradiction check: a native `ClusterVersion` API surface can no longer be persisted under `kubernetes` or `rke2`. This prevents a stale/buggy classifier from bypassing the preview/read-only fence by claiming a supported distribution while OpenShift authority is visible in inventory.
- bind imported-cluster continuity to the physical Kubernetes cluster identity. The Agent re-attests the `kube-system` UID on every inventory and fallback heartbeat; a non-empty mismatch with the UID captured at import claim is rejected before state/liveness can be refreshed. Pre-0.0.205 Agents that omit the UID remain observable but their inventory is server-normalized to read-only.
- make `target-cluster-uid-attested` a server-owned inventory authority marker. Agent-supplied copies of that capability are removed and reconstructed only after the control plane verifies physical identity continuity; mutation RBAC activation is unavailable until this marker exists.
- close cross-cluster RBAC activation leakage. The mutation activation manifest now carries a dedicated `4so-platform-mutation-activation` ConfigMap bound to both the managed-cluster ID and claimed external UID; the Agent verifies that binding against its live `kube-system` UID before running any `SelfSubjectAccessReview`. Applying cluster A's activation manifest to cluster B therefore cannot activate B's mutation capability.
- keep activation inventory references truthful as issuance provenance (`issuedFromInventoryDigest`) rather than pretending the RBAC grant is cryptographically tied to all future inventory digests. Existing operation/task planning still fences execution on the current authoritative inventory digest.
- bound **new task claims** to a server-timestamped three-minute inventory-authority freshness window while keeping result acceptance on the already-issued lease/fence and unchanged authority epoch. A disconnected target can no longer retain mutation claim authority indefinitely, but a legitimate long-running task is not invalidated solely because the inventory freshness window elapsed after it was leased.
- restore Memory/PostgreSQL claim parity for baseline work: `NextBaselineTask` now consumes the common fresh-cluster admission helper, and a recursive helper-call defect introduced during the freshness split is removed. Move the generic PostgreSQL Agent admission validators out of the baseline owner into the fleet/cluster authority owner and rename them `validateClusterAgentTx` / `validateFreshClusterAgentTx`, removing the misleading cross-subsystem baseline coupling that enabled the defect. A scripted PostgreSQL regression proves stale inventory is rejected before task selection, while owner tests prove a still-valid leased result remains reportable after claim freshness expires.
- add negative controls for OCP-vs-OKD classification, native-ClusterVersion/distribution contradiction, non-OKD release versions, legacy read-only inventory, inventory/heartbeat identity mismatch, and cross-cluster activation manifests. Phase C remains blocked only on `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; no runtime or Physical PASS is inferred.

# 0.0.204 — OKD import authority and read-only admission source closure

- close the source-level OKD distribution-identity gap without promoting OKD to a mutation-capable target: the Agent now reads the real `config.openshift.io/v1` `ClusterVersion/version` singleton, binds UID/version evidence, requires complete API/CRD discovery, and rejects a same-name CRD as ambiguous authority; the control plane independently validates that evidence before persisting canonical `okd`.
- stop trusting Agent-supplied inventory digests. Inventory identity evidence is included in the canonical digest and both memory/PostgreSQL stores recompute it before binding the managed cluster, preventing a caller-selected digest from becoming target authority.
- make imported-cluster mutation authorization explicitly two-stage. Enrollment now installs only read-only inventory/credential RBAC; a separate inventory-bound mutation activation manifest is available only to supported distributions. The Agent proves the representative mutation permissions with `SelfSubjectAccessReview` and reports `target-mutation-rbac-active` before any task queue can run.
- enforce the mutation fence on both task claim and result acceptance across baseline, tenant, provider, runtime verification/certification, drift/upgrade and cluster-maintenance paths, including PostgreSQL production semantics. Preview/unsupported targets have mutation claims stripped and receive `target-read-only-admission`; OKD therefore remains read-only even if an Agent attempts to advertise mutation capabilities.
- add a server-owned `inventory_updated_at` authority epoch. A successful inventory report synchronizes inventory and heartbeat authority; a later heartbeat without a fresh inventory breaks mutation admission, so liveness alone cannot preserve stale target identity/RBAC authority. Long-running already-leased work is not invalidated by an arbitrary wall-clock TTL.
- add migration `0046` as rolling-safe additive state for distribution evidence and inventory authority, plus negative controls for OKD evidence incompleteness/CRD spoofing, forged digest rejection, read-only capability stripping, staged RBAC activation and heartbeat-only epoch breakage.
- update `PROGRAM_PHASE_MODEL_V2` truthfully: Phase C remains `blocked`, but its source blockers for authoritative detection and read-only fencing are closed. The only remaining Phase C blocker is `OKD_IMPORT_RUNTIME_CERTIFICATION_PENDING`; no Generated/Installed Runtime, Runtime-Realism, Exact-SHA Physical Runtime or OKD mutation PASS is inferred.

# 0.0.203 — OKD goal phase dependency rebaseline

- rebase the product roadmap after a deep goal-to-code audit instead of advancing implementation on a misleading linear sequence. `PROGRAM_PHASE_MODEL_V2` separates authoritative OKD import detection/read-only admission, inventory-bound capability/health ownership, and desired+observed target-profile/catalog compilation.
- expose the real current blockers in Phase C: the Agent still has no authoritative OpenShift/OKD API detector, imported preview/unsupported targets are not yet fenced from generic mutation claims, and the OKD import path has no runtime certification evidence. No OKD admission or Physical PASS is invented.
- remove the false dependency that made imported OKD Day-2 wait for managed bare-metal installation. Imported Day-2 now branches from observed capability ownership and may proceed in parallel with managed Compact-3 work.
- prevent a pre-install circular dependency by requiring the target-profile compiler to support both desired state before a cluster exists and observed state after import/install. Managed Compact-3 consumes that compiler; it does not require inventory from a cluster that has not been created.
- make managed Compact-3 exit criteria explicit around pinned `openshift-install`/FCOS/Agent ISO inputs and one real `BootMediaProvider`; make disconnected delivery explicitly `oc-mirror` v2 + zot; make upgrades consume CVO-supported update edges and ClusterVersion/ClusterOperator evidence without exposing synthetic rollback.
- add phase-local exit criteria, explicit dependency/parallel metadata, and keep Exact-SHA physical certification as the final independent not-evaluated phase. The Operator Console now labels the roadmap item as the current product phase rather than the next phase.

# 0.0.202 — program phase authority and target capability foundation closure

- correct the product roadmap around the target-architecture goal instead of treating the current 18-component generic blueprint as the universal prerequisite for every future target. `PROGRAM_PHASE_MODEL_V1` now makes the canonical order explicit: architecture rebaseline -> target/capability foundation -> first-class OKD import -> OKD-aware catalog/profile -> managed Compact-3 -> Day-2 -> disconnected -> CVO upgrade -> Exact-SHA physical certification. No percentage or physical success is inferred from source implementation.
- add `TARGET_CAPABILITY_RESOLVER_V1` as the source-level resolver boundary. Kubernetes and RKE2 remain admitted; OKD stays preview-only/fail-closed. The OKD preview suppresses default duplicate Cilium, Capsule and VictoriaMetrics selections, makes Kyverno/MetalLB/snapshot-controller/Tetragon conditional on target capabilities/dependencies, and never converts that preview into OKD admission.
- expose the resolver descriptor and canonical program roadmap through the existing target-architecture API and embed the same roadmap in `platformctl release-readiness`, separating product-program sequencing from the existing selected-blueprint source/runtime/deployment gate counts.
- make the Provider Operator Console consume the target-architecture authority so admitted/preview distributions, resolver authority and the next product phase are live API data instead of hard-coded UI prose. This closes the source-semantics foundation phase while keeping `C-okd-import-first-class` as the next blocked objective.
- preserve the existing 76 selected-blueprint blockers and `Exact-SHA Physical Runtime=not-evaluated`; source acquisition/runtime certification are still truthful deployment gates, but they are no longer presented as the architectural phase ordering for first-class OKD support.

# 0.0.201 — disaster recovery preflight archive admission closure

- close a destructive DR admission gap left after 0.0.200: restore preflight proved object existence and SHA-256 before workload quiesce, but structural archive safety was only checked later inside each restore Job. A checksum-valid unsafe archive could therefore pass preflight, quiesce all platform workloads, then fail before extraction and unnecessarily force the appliance into recovery-required state.
- make the pre-quiesce restore preflight run the same canonical tar safety contract over every downloaded PostgreSQL, platform-secrets, agent-PKI, Forgejo and zot payload after digest verification and before deleting its staged copy.
- preserve in-restore archive verification as defense-in-depth; no payload is trusted solely because preflight passed, and no source/runtime certification or Exact-SHA Physical Runtime evidence is promoted.
- add owner regression assertions proving preflight orders digest verification -> archive structural admission -> cleanup for the component loop.

# 0.0.200 — lifecycle restore archive command safety closure

- close a real restore-command inversion in the archive safety gate: the previous `! tar -tvzf | awk ...` pipeline inverted the intended result, so safe archives could be rejected while archives containing rejected link types could continue into extraction.
- make archive admission positively require GNU tar member types to be regular files or directories only; reject symlinks, hard links, FIFOs, block/character devices and other special entries before extraction.
- reject setuid/setgid regular-file modes before extraction so a crafted but checksum-valid backup cannot introduce privileged executables into restored application data.
- add executable owner regressions using real tar archives for safe regular/directory content, FIFO entries and setuid files, plus preserve the existing path-traversal and restore-order checks.
- keep checksum/authenticity, restore staging, lifecycle fencing, source acquisition and Exact-SHA Physical Runtime semantics unchanged; no certification or physical evidence is synthesized.

# 0.0.199 — lifecycle restore archive safety closure

- Hardened disaster-recovery restore archive admission against absolute/parent-traversal members and symlink/hardlink entries before extraction.
- Applied the same archive safety boundary to PostgreSQL, platform-secrets, agent-PKI, Forgejo, and zot restore paths.
- Restore remains fail-closed before destructive extraction/mutation when archive structure is unsafe.

# Changelog

## 0.0.198 — Lifecycle restore Job payload staging integrity closure

- close a destructive restore time-of-check/time-of-use gap left after 0.0.197: lifecycle restore admission verified backup metadata and payload before quiesce and again after quiesce, but the Kubernetes restore Job later reopened the mutable hostPath directly for `pg_restore` and PVC extraction without re-verifying the bytes it was about to consume. A payload changed after host-side validation could therefore become the destructive restore input.
- add an in-Job `verify-backup` init container that re-checks every metadata-authorized payload for regular/non-symlink shape, exact byte size and SHA-256 on the execution node, copies the verified bytes into a pod-local `emptyDir`, and verifies the staged copy again before any database or filesystem mutation starts.
- make PostgreSQL restore and PVC extraction consume only the verified pod-local staging volume (`/verified/database.dump` and `/verified/data.tar.gz`); the mutable backup hostPath is mounted read-only only into the verification init container. This preserves retry/recovery semantics while removing the hostPath TOCTOU boundary from destructive mutation.
- add owner regression coverage proving Forgejo restore manifests contain the verification/staging contract and that both database and data restore paths consume only staged bytes. Exact-SHA Physical Runtime remains an independent gate.

## 0.0.197 — Lifecycle backup restore admission integrity closure

- close a destructive-restore admission gap where lifecycle backup metadata with zero payload files could pass `validateBackupForRestore`, allowing a restore to quiesce a workload before the fixed restore Job discovered that the required payload was absent and forced the operation into recovery-required state.
- make backup payload authority service-specific and exact before destructive mutation: Forgejo requires `data.tar.gz` + `database.dump`, Keycloak requires `database.dump` + `marker`, and zot requires `data.tar.gz`; metadata must contain exactly that set with unique canonical names, valid SHA-256 digests, positive sizes and byte-for-byte size/digest agreement.
- reject symlink/special-file backup payloads and symlinked metadata/directories, bind opened files to the pre-open inode, and reject path-escape/extra entries so metadata cannot make restore admission hash or trust bytes outside the product-owned backup directory.
- make lifecycle simulation generate the same payload filenames as the real backup Jobs instead of the former `database.sql`/generic data shape, so restore/recovery tests exercise the production payload contract rather than a parallel simulated format. Add owner regressions for empty metadata, path escape, matching-digest symlink payload and size-authority mismatch. Exact-SHA Physical Runtime remains a separate gate.

## 0.0.196 — Planner immutable Git revision authority closure

- close a planner-authority bypass where `revisionType=commit` was treated as sufficient proof of immutability even when `spec.delivery.revision` was `main`, a tag-like value, malformed hex or a shortened object ID; `BuildWithCertificationAuthority` could therefore manufacture `execution-ready` when called with already-authorized catalog evidence outside the normal blueprint validator path.
- make the deployment planner independently require a full immutable Git object ID using the same 40-character SHA-1 / 64-character SHA-256 contract as blueprint validation and Git delivery; caller validation is now defense-in-depth rather than a prerequisite for planner safety.
- add owner regressions proving invalid commit-typed revisions remain planning-only even with certification authority, while full 64-character SHA-256 Git object IDs remain execution-eligible. No source lock, runtime certification or Exact-SHA Physical Runtime evidence is fabricated.

## 0.0.195 — OCI mirror transport and upload-location closure

- close an OCI mirror transport-floor gap below the authenticated/control-plane redirect closure: an HTTPS registry could return an absolute HTTP blob-upload `Location`, causing Platform Factory to transmit immutable image-layer bytes over plaintext even though the configured registry authority was TLS-protected. Registry redirects and upload locations now fail closed on HTTPS→HTTP downgrade while preserving OCI-compatible HTTPS offload/CDN locations.
- preserve opaque registry upload-session authority exactly: critical query parameters from the registry `Location` are no longer parsed, reordered and re-encoded before the final PUT. Platform Factory now leaves the existing raw query byte-for-byte intact and appends only the required `digest` parameter, rejecting a conflicting pre-existing digest instead of silently rewriting it.
- resolve relative upload locations against the final successful POST response URL after any permitted redirect, rather than the original upload-initiation URL, and reject registry base URLs decorated with embedded credentials, path, query or fragment so the configured registry authority has one unambiguous origin contract.
- add stable owner regressions reproducing plaintext upload downgrade, signed-query mutation and post-redirect relative-location misresolution. No catalog source, runtime certification or Exact-SHA Physical Runtime state is promoted by this release.

## 0.0.194 — Upstream transport authority and redirect closure

- close an immutable Helm acquisition integrity-anchor gap: an admitted HTTPS chart repository index could previously follow a redirect to HTTP (or another non-HTTPS scheme), allowing the upstream index that supplies the expected chart digest to cross a TLS downgrade boundary before the downloaded chart was verified against it. The acquisition owner now validates the effective response URL and fails closed on any non-HTTPS final transport while still allowing HTTPS CDN redirects.
- close the equivalent authenticated control-plane redirect boundary in `platformctl`: evidence downloads, installer field campaigns, diagnostics, token/access operations and zero-to-HA flows all share the canonical HTTP client, which now refuses redirects instead of allowing endpoint authority to change after an HTTPS/loopback base URL was validated. This prevents Bearer/bootstrap credentials from being forwarded through redirect chains and makes the configured endpoint the only network authority.
- add stable owner regressions for HTTPS index downgrade denial and authenticated redirect refusal before credential forwarding; preserve all existing upstream admission, source acquisition, runtime certification and Exact-SHA Physical Runtime truthfulness without promoting any component or fabricating evidence.

## 0.0.193 — Release readiness upstream authority parity closure

- close a release-phase authority drift where the Go `release-readiness` path trusted a much weaker interpretation of embedded `upstream-admission.json` than the product-owned acquisition validator; a stale admission row could therefore report the upstream-admission phase as complete even when its constraint/version no longer matched the live Catalog.
- add one Go authority validator shared by ReleaseReadiness that fail-closes on policy drift, unresolved-Helm coverage mismatch, unsupported admission status, chart/source mismatch, missing rationale, invalid selected/upstream version relationships, review-constraint drift, ready-candidate Catalog pin drift and ready-candidate version-policy drift.
- preserve phase truthfulness: the enterprise baseline remains 16 admission rows with 9 ready / 7 review-required and the existing immutable-source/runtime/physical blockers remain unchanged; this release does not synthesize acquisition, certification or Exact-SHA Physical Runtime evidence.
- add stable owner regressions proving the embedded Catalog and embedded upstream admission authority remain mutually valid and that stale constraint or ready-pin drift is rejected before a ReleaseReadiness report is emitted.

## 0.0.192 — Release phase authority and Autopilot convergence closure

- add `platformctl release-readiness -f <blueprint>` as the canonical machine-readable release-phase authority. It derives product blockers, deployment-context blockers, upstream-admission state, immutable source-acquisition state, runtime-certification state and control-plane certification-authority reachability from the same live catalog/plan contracts instead of creating a parallel roadmap/state document.
- make unknown future plan blockers fail closed as product-release blockers, keep only immutable Git revision as the current post-publication deployment-context blocker, and make the Exact-SHA Physical Runtime phase explicitly `not-evaluated`; this command cannot manufacture or infer Physical PASS from source/build/runtime metadata.
- remove Autopilot's private release-blocker classifier and make release/real-test decisions consume the canonical CLI readiness authority, preventing planner/Autopilot phase drift while preserving the distinction between local code correctness, external supply-chain closure and physical execution.
- close a Planner authority-suppression edge case: when component source/runtime gates were fully satisfied but the selected Git revision was still mutable, the deployment-context blocker previously prevented `CERTIFICATION_AUTHORITY_UNVERIFIED` from being emitted. Certification authority is now evaluated once component readiness is complete, independently of the post-publication Git immutability gate.
- embed and strictly parse `catalog/upstream-admission.json` in the product catalog authority so phase reporting is bound to the same admitted upstream review rows as acquisition. Unresolved enabled components without an admission authority now fail closed in readiness evaluation.
- correct the Loki upstream review after the 2026 OSS chart ownership migration: the OSS chart now belongs to `grafana-community/helm-charts` with a changed chart-version line, while the shipped 7.3.x path is maintained for Grafana Enterprise Logs. Loki therefore remains architecture-review-required until the product-owned OSS delivery contract is deliberately rebaselined; no source-resolution or runtime-certification evidence is fabricated.
- close a release-gate liveness defect exposed by the UI smoke lifecycle: teardown through implicit Playwright context/finalization could leave gate completion ambiguous in this environment, while hard process exit could sever the driver pipe and emit EPIPE. Headless and live UI smoke now own Playwright start/stop explicitly, close browser/context first, persist evidence, and return the real exit code without orphaning the driver; live smoke also guarantees platform-api cleanup if driver shutdown fails.
- preserve truthful phase state for the enterprise baseline: current external acquisition/runtime-certification blockers remain blockers until real immutable acquisition and target-runtime evidence exist, and Exact-SHA Physical Runtime remains a separate gate.

## 0.0.191 — Catalog install durable crash residue closure

- close a crash-recovery liveness gap in repair of an already-created immutable catalog runtime bundle: a process death inside `durablefile.Replace` could leave its reserved `.durable-*` temp beside a missing payload, causing every retry to reject the otherwise recoverable runtime directory as containing an unexpected file.
- while holding the repository catalog-install transaction fence, remove only real regular product-owned `.durable-*` residue from the final runtime bundle and from the catalog/component authority directories; special-file or symlink lookalikes remain fail-closed, and directory synchronization makes cleanup durable before mutation continues.
- prevent the same crash residue from silently entering a release artifact: the canonical repository validator reports stale durable temps outside excluded runtime state, and the deterministic release builder independently refuses to package them even when invoked directly.
- add release-neutral owner regressions reproducing the 0.0.190 retry deadlock, catalog/admission metadata residue and hostile symlink cleanup case, plus a release-source boundary regression. Exact-SHA Physical Runtime remains a separate gate.

## 0.0.190 — Catalog install runtime file integrity closure

- close the remaining existing-runtime path-integrity gap in catalog install: an expected immutable runtime payload name such as `artifact.bin`, `provenance.json` or `source-lock.json` could be a symlink to a matching file outside the repository and 0.0.189 would follow it during idempotent replay, treating externally-owned bytes as a valid repository runtime bundle.
- require every already-present runtime bundle payload to be a real regular file before comparison/repair; expected symlinks and other special files now fail before the component contract or Helm admission authority can be mutated, while genuinely missing files in an otherwise matching partial bundle retain the existing crash-repair behavior.
- harden repository-owned regular-file reads with a no-follow/nonblocking final open plus post-open `fstat`, removing the final-path check/open substitution window for catalog component/admission reads and preventing a substituted FIFO from turning an install into an unbounded read.
- add a release-neutral owner regression that reproduces the 0.0.189 symlink acceptance and proves rejection occurs before catalog/admission mutation. Exact-SHA Physical Runtime remains a separate gate.

## 0.0.189 — Catalog install authority path and crash hygiene closure

- close a canonical-authority path-integrity bypass in Helm upstream admission: acquisition/validation/install no longer follow a symlinked `catalog/upstream-admission.json` (or symlinked catalog/components authority path) as though it were repository-owned truth; the canonical execution boundary must now be a real in-repository regular file beneath real repository directories before any tool/network use or catalog mutation.
- close catalog install filesystem escape surfaces by rejecting symlinked component contracts and symlinked runtime component/destination directories before immutable bundle writes, including non-Helm external tagged-source installs that do not traverse the Helm admission coverage path.
- close the remaining pre-rename crash hygiene gap in immutable runtime installation: stale product-owned `.<version>.partial-*` staging directories left by a dead process are removed under the repository install fence before retry, with the runtime parent synchronized before the transaction continues.
- add release-neutral owner regressions for symlinked canonical admission, symlinked external component authority, symlinked runtime parents and stale crash staging; retain strict global admission parity, repository-wide install serialization and 0.0.188 post-component/pre-retirement recovery semantics. Exact-SHA Physical Runtime remains a separate gate.

## 0.0.188 — Catalog install crash recovery closure

- close the post-component/pre-admission-retirement crash deadlock in Helm catalog import: 0.0.187 could durably commit the immutable runtime bundle and resolved component contract, crash before retiring the consumed admission row, then reject every retry because strict global coverage observed one stale authority row for a component that was no longer unresolved.
- add a narrowly-scoped resolved-import recovery check that accepts only the exact interrupted transaction shape: the current component must already equal the verified resolved bundle contract, the stale target row must still exactly match the admitted chart/source/version/revision, and removing only that row must make the remaining canonical authority strictly cover every unresolved Helm component. Any unrelated malformed/missing/extra authority state still fails before mutation.
- preserve repository-scoped install fencing, immutable runtime replay and global admission parity; a retry of the interrupted import now validates/repairs the runtime bundle, replays the identical component write and retires the stale row so the repository returns to canonical validity without manual authority editing.
- add a crash-state owner regression that fails on 0.0.187 with `authority=1 unresolved-helm=0` and proves idempotent recovery plus post-recovery strict admission validation on 0.0.188. Exact-SHA Physical Runtime remains a separate gate.

## 0.0.187 — Catalog install global admission parity closure

- close a validator/runtime trust-boundary gap in `catalog-bundle install`: 0.0.186 verified only the target Helm admission row, so a target-valid bundle could mutate runtime/component state even while another row in the shared canonical `catalog/upstream-admission.json` was malformed or the authority no longer exactly covered the unresolved Helm catalog.
- make the Go install owner validate the complete canonical admission authority against every unresolved Helm component before any filesystem mutation, including exact coverage, component identity, status, release constraint, chart, HTTPS/OCI source shape, rationale, selected/upstream version parity, exact ready-component catalog pin and version-policy semantics.
- fail closed on malformed/symlinked component contracts while constructing the admission coverage authority, preventing install from proceeding against a repository state that the canonical release validator would reject.
- add a negative owner regression that reproduces 0.0.186 accepting a valid target row beside an invalid unrelated admission row and proves 0.0.187 rejects the authority before runtime or component mutation. Existing concurrency fencing, idempotent replay and admission retirement semantics remain unchanged; Exact-SHA Physical Runtime remains a separate gate.

## 0.0.186 — Catalog install concurrency transaction fencing closure

- close a repository-authority lost-update race in concurrent `catalog-bundle install` operations: two different Helm imports could both verify against the same `catalog/upstream-admission.json`, commit their independent runtime/component payloads, then race while retiring the shared authority document so the later atomic rename resurrected an already-consumed row and left the repository invalid.
- serialize the complete repository install transaction (`component/admission read → contract verification → immutable runtime commit → component commit → admission retirement`) with a repository-scoped advisory lock under excluded `.state/catalog-bundle-install.lock`; process crash releases the lock, existing crash/retry idempotency remains intact, and the lock never enters deterministic release artifacts.
- fail closed when the transaction state path is not a real directory or the lock path is not a regular file instead of following an unexpected symlink/special-file lock target.
- add an eight-way concurrent install regression that reproduces the stale-admission row on 0.0.185 and proves all independent imports commit while every consumed row is retired under the fenced transaction. No component source/certification state is promoted by this release; Exact-SHA Physical Runtime remains a separate gate.

## 0.0.185 — Catalog install admission transaction closure

- close the remaining Helm admission bypass below `acquire_upstream_helm.py`: `platformctl catalog-bundle install` now binds unresolved Helm imports to the canonical repository `catalog/upstream-admission.json` row and rejects review-required, source/version/revision/chart or policy mismatches before any runtime/component filesystem mutation.
- bind an imported bundle to the repository's existing unresolved component contract. Import may only perform the explicit source-resolution transition (`release` exact-pin, offline repository key, immutable source evidence and candidate certification reset); it can no longer substitute support tier, compatibility, dependencies, namespace, operations or other catalog policy while resolving the same component name.
- make successful Helm import retire its consumed admission row after runtime bundle and component durability are established, so the authority continues to cover exactly the remaining unresolved Helm set and post-install repository validation can PASS. The ordering is retry-safe: a crash before component commit keeps the row; a crash after component commit can idempotently retire the stale row; a completed import can be replayed after the row is already absent.
- add owner regressions for review-required direct bundle install, unresolved component-contract substitution, admission retirement and idempotent replay. No component is promoted in this release; the existing 9 ready / 7 review-required acquisition queue remains unchanged until real upstream artifacts are acquired. Exact-SHA Physical Runtime remains a separate gate.

## 0.0.184 — Fresh artifact deterministic Go gate closure

- close the remaining release-gate orchestration defect reproduced only from a freshly extracted exact artifact: both `go test -p=2 ./...` and `go test -p=1 ./...` can stall between packages even though every owner package passes in an isolated Go invocation.
- run Go unit, vet and race gates package-isolated in both the canonical Makefile and `verify_release.py --full`; the package set is still derived from the complete `go list ./...` authority and unit/race keep `-count=1`.
- preserve the 0.0.183 upstream-acquisition authority closure unchanged; no catalog component is promoted and no runtime certification state is widened.
- Exact-SHA Physical Runtime remains a separate gate and is never inferred from source, packaged-binary or smoke verification.

## 0.0.183 — Exact artifact upstream authority and release gate closure

- materialize the upstream-acquisition authority bypass closure in the actual release source: unresolved Helm acquisition now requires `--from-admission`, rejects non-canonical `--authority` files before Helm/Crane/network use, and treats version/source flags only as consistency assertions against the canonical `catalog/upstream-admission.json` row.
- add owner regressions proving both direct version/source acquisition and alternate-authority acquisition fail before external tool or network access.
- use bounded Go scheduler concurrency (`-p=2`) for unit/vet gates after reproducing that unrestricted cross-package execution can stall while fully isolated execution adds excessive process/link overhead; race remains package-isolated because the race build still reproduces cross-package stalls even at `-p=2`. The full `go list ./...` package set and fresh `-count=1` test execution are retained.
- this release changes no runtime catalog certification state and does not infer Exact-SHA Physical Runtime from source, generated-artifact or smoke gates.

## 0.0.182 — Release gate serialization and upstream authority closure

- preserve the 0.0.181 upstream-acquisition authority bypass closure and additionally close a release-gate orchestration defect reproduced from a freshly extracted exact release artifact: parallel cross-package `go test ./...` / `go test -race ./...` could stall under shared runtime/filesystem pressure even though every owner package passes independently.
- serialize the canonical source Go unit, vet and race package scheduler one package per process; unit/race execution uses `-count=1` so the gate exercises fresh test executions without reducing the package set or weakening assertions.
- make the fresh extracted-artifact verifier use the exact same one-package-per-process strategy for unit, vet and race phases; the exact artifact verifier therefore covers the full `go list ./...` set without cross-package resource contention becoming an unbounded false blocker.
- no product runtime behavior, catalog resolution status, certification state or component scope is widened. Exact-SHA Physical Runtime remains a separate gate and is not inferred from source/smoke success.

## 0.0.181 — Upstream acquisition authority bypass closure

- make the product-owned `CatalogUpstreamAdmission` a mandatory execution boundary for unresolved Helm acquisition: direct `--version/--source` acquisition without `--from-admission` now fails before Helm, Crane or network use instead of bypassing the authority introduced in 0.0.180.
- reject alternate `--authority` files during acquisition so a release engineer cannot point `--from-admission` at an arbitrary external JSON document that self-approves an untrusted chart source; acquisition is bound to the canonical repository authority at `catalog/upstream-admission.json`.
- retain `--version`, `--upstream-version` and `--source` only as optional consistency assertions against the canonical admitted row; mismatches continue to fail closed and review-required components remain non-executable.
- add owner regressions for both bypass paths and preserve the existing separation between exact version admission, immutable source acquisition and runtime certification. No component is promoted to source-resolved or runtime-certified by this closure.

## 0.0.180 — Catalog upstream admission and acquisition authority foundation

- begin PHASE 2 Catalog Execution Closure with one machine-readable `CatalogUpstreamAdmission` authority covering all 16 unresolved Helm components; it separates official-source/exact-version selection from immutable source acquisition and runtime certification instead of allowing ad-hoc release-engineer URL/version choices.
- exact-pin the nine components whose reviewed upstream candidate is currently admitted for acquisition (`alloy`, `argocd`, `capsule`, `cert-manager`, `external-dns`, `external-secrets`, `tetragon`, `velero`, `victoria-metrics`) while deliberately keeping `source.resolved=false`, source-lock/digest fields empty and runtime certification at `candidate`; this removes nine real `COMPONENT_VERSION_NOT_PINNED` blockers without fabricating supply-chain or runtime evidence.
- keep seven components fail-closed behind explicit review states rather than silently widening or modernizing the baseline: Ceph-CSI requires a product architecture decision because upstream recommends the Operator path for new deployments; Cilium and kgateway require Gateway API dependency alignment; Grafana, Loki, Kyverno and MetalLB still require an exact admitted patch selection.
- add `scripts/catalog_upstream_admission.py`, repository invariants and owner regressions that require exact coverage, official HTTPS/OCI sources, exact non-prerelease versions for ready rows, unchanged constraints for review rows, and strict separation between version admission, source resolution and certification evidence.
- extend `scripts/acquire_upstream_helm.py` with `--from-admission`: release engineering can acquire/install a ready component without repeating version/source arguments, while overrides and review-required components are rejected before Helm/Crane or network use. Add Make targets for authority validation/planning and an admission-aware acquisition preflight.
- preserve truthful planning semantics: the enterprise Blueprint remains `planning-only`; its canonical blocker count moves from 85 to 76 solely because nine versions are now exact-selected. The remaining unresolved-source/source-lock/runtime-certification evidence and immutable Git revision blockers remain authoritative.

## 0.0.179 — Target architecture model rebaseline foundation

- introduce `TARGET_ARCHITECTURE_MODEL_V1` as the product-owned boundary that separates Kubernetes distribution identity, provisioning mode, infrastructure provider and provisioning adapter instead of overloading one `distribution/provider` pair with unrelated meanings; the internal RKE2 management appliance remains an explicit management-plane boundary rather than a generic target abstraction.
- canonicalize distribution identities to `kubernetes` and `rke2` for currently admitted targets; legacy `generic-imported` and `kubespray` values remain accepted and normalize to `kubernetes`, while `okd` is only recognized and explicitly not admitted until the dedicated OKD import/capability-resolution phases exist.
- expose `GET /api/v1/target-architecture-model`, enrich imported-cluster API responses with an explicit target model, and expose canonical target metadata on Provider Profile/Provider Cluster surfaces; Cluster API is now represented as `provisioningMode=cluster-api` rather than a distribution or infrastructure identity.
- migrate the shipped component catalog and enterprise Blueprint to canonical distribution identities, add rolling-safe migration 45 to canonicalize mutable Provider Profile aliases/defaults, and preserve legacy immutable release compatibility through compatibility-engine normalization rather than history rewriting.
- update the Operator Console terminology so operators choose a Distribution identity separately from the Provisioning adapter; add release-neutral regressions for legacy normalization, the target-model API, provider target metadata and migration fencing.

## 0.0.178 — Agent task deterministic ordering parity closure

- close a backend-parity defect across six durable Agent/worker queues where Memory/FileStore selected equal-time candidates from Go map iteration while PostgreSQL used a stable ID tie-break: Baseline Deployment, Runtime Verification, Runtime Certification, Provider Profile verification, Provider Cluster execution, and Drift Scan; each queue now uses the same canonical PostgreSQL tuple (`createdAt ASC, id ASC`, or `updatedAt ASC, id ASC` for Provider Cluster execution).
- align the corresponding operator-facing Memory/FileStore collections for Baseline Deployment, Runtime Verification, Runtime Certification, Provider Profile, Provider Cluster and Drift Scan with PostgreSQL equal-time ordering, removing backend-dependent list chronology without changing lifecycle, approval, recovery, task lease or fence-token semantics.
- centralize stable ResourceMeta comparison so equal timestamps cannot fall back to map iteration; FileStore inherits the same ordering after durable snapshot restore.
- add release-neutral regressions that repeatedly fail on 0.0.177 for all six first-task claims, verify deterministic list ordering, and prove Baseline task selection remains canonical after FileStore restart.

## 0.0.177 — Outbox availability ordering and lease-fencing closure

- close a scheduler-parity defect where Memory/FileStore claimed eligible outbox rows by random resource ID while PostgreSQL selected by `available_at,id`; an event that became available later could therefore jump ahead of older available work. All built-in authority adapters now expose the same `AvailableAt ASC, ID ASC` claim order, including equal-time ties and FileStore restart.
- normalize PostgreSQL claim return order after `UPDATE ... RETURNING`: candidate membership was already selected by `available_at,id`, but PostgreSQL does not guarantee RETURNING row order, so the store boundary now returns the claimed batch in the same deterministic scheduler order as Memory/FileStore.
- close an outbox lease-fencing defect in the notification dispatcher where `MarkOutboxPublished` reused the timestamp captured before claiming the batch. Publication now checks lease ownership using the actual completion time, so work that outlives the 30-second claim lease fails with `ErrStaleFence` instead of being published under an expired lease.
- add regressions that fail on 0.0.176 for older-available-work selection and expired-lease publication, plus equal-availability, FileStore restart and PostgreSQL unordered-RETURNING regressions. No notification routing policy or delivery lifecycle behavior is widened.

## 0.0.176 — Tenant deterministic task ordering parity closure

- close a backend-parity defect where Tenant collections sharing the same `createdAt` were ordered by Go map iteration in Memory/FileStore while PostgreSQL used `created_at,id`; equal-time tenant lists are now canonical `createdAt ASC, id ASC` across all built-in authority backends;
- close the same deterministic-selection gap in the tenant Agent queue: `NextTenantTask` now uses the PostgreSQL claim tuple (`createdAt ASC`, `id ASC`) so equally old runnable tenants converge on the same first task across Memory, FileStore, PostgreSQL and restarts;
- keep tenant lifecycle, leases/fence tokens, protected delete, runtime-contract reconciliation, approval and recovery semantics unchanged; the closure only removes map-order authority from list/claim chronology;
- add regressions that fail on 0.0.175 for equal-time collection ordering and first task claim, plus a FileStore restart regression proving deterministic chronology survives durable restore.

## 0.0.175 — Cluster maintenance deterministic claim ordering parity closure

- close a backend-parity defect where multiple Cluster Maintenance runs created in the same clock tick shared `createdAt`, but Memory/FileStore sorted only by creation time while PostgreSQL used `created_at,id`; equal-time operator-facing run lists could therefore change order across requests/restarts based on Go map iteration;
- close the same tie defect in maintenance worker claiming: Memory/FileStore now use the PostgreSQL queue tuple (`createdAt ASC`, `id ASC`) so two equally old queued runs deterministically select the same first task on every built-in authority backend;
- keep maintenance window, inventory digest/node-UID fencing, approval separation, operation leases and failure recovery semantics unchanged; this closure only removes map-order authority from list/claim chronology;
- add regressions that fail on 0.0.174 for equal-time list ordering and first task selection, plus a FileStore restart regression proving deterministic chronology survives durable snapshot restore.

## 0.0.174 — Notification delivery deterministic ordering parity closure

- close a backend-parity and bounded-page stability defect where notification deliveries created by one routed event share the same `createdAt`, but Memory/FileStore used no ID tie-break and therefore returned map-order-dependent subsets for `limit=N` while PostgreSQL used `created_at DESC,id DESC`; built-in authorities now return the same deterministic operator-facing delivery chronology;
- close the same tie defect in worker claiming: Memory/FileStore now match PostgreSQL claim order exactly (`next_attempt_at ASC`, `created_at ASC`, `id ASC`) so equal-time eligible deliveries are selected deterministically across restarts/backends rather than from Go map iteration order;
- keep scoped notification event/delivery authorization-before-limit behavior unchanged; the closure only canonicalizes equal-timestamp ordering and does not widen scope or reintroduce global scans/N+1 lookups;
- add regressions that repeatedly reproduce the unstable bounded subset and non-canonical first claim on the previous implementation, then pass only with the stable ID tie-breakers.


## 0.0.173 — Operator collection chronology parity closure

- close bounded Operations chronology drift where PostgreSQL returned the operator-facing page newest-first while Memory/FileStore fell back to the legacy full-list order and returned the same page oldest-first; add the same bounded pager contract to Memory/FileStore with authorization-before-limit and the PostgreSQL ordering tuple (`updatedAt DESC`, `createdAt DESC`, stable ID tie-break);
- keep legacy unbounded `ListOperations` semantics unchanged for internal reconciliation/snapshot callers, while making the interactive `limit` path canonical across all built-in authority adapters;
- close Security Audit backend parity in the opposite direction: PostgreSQL still selects the latest bounded tail efficiently with `sequence DESC LIMIT`, but now returns that tail in sequence-ascending hash-chain order like Memory/FileStore so `ValidateSecurityAuditChain` consumers see the same valid chronology on every backend;
- add regressions that fail on 0.0.172 for Memory bounded-operation ordering and PostgreSQL Security Audit chain ordering, then pass only after the owner-store fixes.

## 0.0.172 — Audit authorized bounded ordering parity closure

- close a high-load scoped Audit defect where `/api/v1/audit-events` materialized the complete control-plane Snapshot and filtered it in the API for every non-platform-admin request; scoped Audit now uses an authority-aware store page contract so unrelated tenant state is not cloned merely to resolve the requested history page;
- add Memory/FileStore scoped Audit paging that resolves the same Organization/Project resource families as the API scope model without taking a global Snapshot, and add a PostgreSQL CTE that resolves scoped resource ownership plus metadata scope inside SQL before ordering and `LIMIT`;
- normalize Audit ordering to newest-first (`occurred_at DESC`, stable ID tie-break) across Memory, FileStore and PostgreSQL so the same endpoint no longer changes chronology with the authority backend;
- add regressions proving scoped API requests use the bounded pager rather than Snapshot, Memory ordering matches the PostgreSQL contract, and the production PostgreSQL query applies authorization before `LIMIT`.

## 0.0.171 — PostgreSQL notification NULL-project scope parity closure

- Fix scoped PostgreSQL notification event pagination to treat organization-wide rows using the schema-authoritative `project_id IS NULL` representation instead of the impossible `project_id=''` predicate.
- Apply the same NULL-project scope correction to notification delivery pagination through the joined event scope, restoring Memory/File/PostgreSQL parity without reintroducing global scans or per-row event lookups.
- Keep authorization-before-limit semantics intact for project-scoped and organization-wide notification collections.

## 0.0.170 — Notification authorized bounded pagination closure

- close a high-load notification collection defect where interactive scoped principals requested `notification-events`/`notification-deliveries` without an explicit Project filter and the API intentionally disabled the store limit, scanned global notification history, then tenant-filtered in process; authorized data was correct but request cost grew with unrelated tenant traffic;
- eliminate the Notification Delivery N+1 authorization path that loaded global deliveries and then called `GetNotificationEvent` once per row merely to discover each delivery's tenant scope;
- add scope-aware bounded notification page contracts to Memory/FileStore and PostgreSQL so Project-scoped events plus Organization-wide events authorized to the principal are selected inside the authority store before ordering and `LIMIT`; PostgreSQL performs the delivery scope join in one query;
- add regressions proving scoped notification collections use the bounded authority-aware path without global scans/per-row event lookups, plus a production PostgreSQL query-contract regression that pins authorization predicates ahead of `LIMIT`.

## 0.0.169 — Project scope isolation and authorized pagination closure

- close a tenant-isolation defect where an interactive principal with a direct role on one Project inherited visibility of the parent Organization for navigation and that visibility was then incorrectly expanded back into read access for every sibling Project in the Organization; Project collection and operation authority now derive from backend-effective Project roles rather than hierarchy visibility;
- separate parent-Organization navigation visibility from Organization-resource authority so a direct Project grant cannot enumerate sibling Organization-owned notification destinations, private Catalog trust/release resources, entitlement/OEM summary counters or Organization-wide audit events that the corresponding detail endpoints would reject;
- apply operation authorization before collection limiting: scoped principals no longer lose older authorized operations when newer foreign-tenant operations occupy the global `limit`; PostgreSQL now performs the authorized Project-set predicate inside the bounded ordered query before `LIMIT`, while non-paged stores filter before slicing;
- add negative regressions for sibling Project operation/metadata isolation, Organization-resource/audit/summary isolation, authorization-before-pagination semantics, and the production PostgreSQL scoped-page query boundary.

## 0.0.168 — Drift remediation idempotent recovery closure

- close a durable retry gap in drift remediation where `POST /api/v1/drift-scans/{id}/targets/{clusterId}/findings/{fingerprint}/remediate` could persist its idempotent `Operation` in `DRAFT` or `PLANNING` and then, after a lost response/process interruption, replay the same key without ever advancing that operation to `QUEUED`;
- make the remediation workflow treat idempotent replay as a recovery path: the same durable operation is monotonically converged through only the safe pre-queue states (`DRAFT → PLANNING → QUEUED`) and already-advanced/terminal authority is never rewound;
- tolerate concurrent identical retries by refreshing the operation after optimistic-concurrency conflict and converging on the authoritative revision instead of returning a spurious 409 solely because two requests raced on the same idempotency key;
- add regressions reproducing the stranded-operation behavior on the previous implementation and proving lost-response recovery plus concurrent stale-revision convergence on exactly one queued operation.

## 0.0.167 — Governed release atomic revision authority closure

- close Catalog and Blueprint paired-write failure boundaries where API create/draft-update/clone flows persisted an immutable revision before the governed release mutation was known to succeed; stale `If-Match`, duplicate identity, validation or later transaction failures could therefore leave orphan revisions plus audit/outbox noise;
- introduce atomic revision+release owner-store operations for Catalog and Blueprint create/draft update, with MemoryStore committing only after all release validation succeeds, FileStore persisting the pair as one rollback-capable snapshot mutation, and PostgreSQL performing the revision plus release mutation in one serializable database transaction;
- move stale revision/state checks ahead of revision creation while retaining database row locking for race-safe optimistic concurrency; Blueprint clone now uses the same paired authority boundary as normal creation;
- add regressions proving stale Catalog/Blueprint draft updates and duplicate release creation do not persist revisions, audit or outbox side effects, plus a FileStore restart regression and a PostgreSQL transaction-contract regression.

## 0.0.166 — Cluster maintenance atomic request closure

- close a composite-workflow failure boundary where `POST /api/v1/clusters/{id}/maintenance-runs` created and advanced a durable `Operation` before maintenance-window, live-inventory and node-selection validation completed; rejected requests could therefore leave orphan `AWAITING_APPROVAL` operations and consume the operation idempotency authority;
- make maintenance request admission atomic in all three authority adapters: MemoryStore validates the run before creating its linked operation under one lock, FileStore persists/rolls back the combined mutation as one snapshot transaction, and PostgreSQL creates/reuses the operation plus maintenance run in one serializable database transaction;
- preserve operation audit/outbox transitions (`DRAFT → PLANNING → AWAITING_APPROVAL`) inside the atomic boundary and safely reuse a matching legacy partial operation when it is still in a recoverable pre-approval state;
- add an owner API regression proving an invalid node request creates no operation and that a corrected retry with the same idempotency key succeeds with exactly one linked approval-bound operation.

## 0.0.165 — Cluster enrollment expiry authority closure

- make enrollment TTL authoritative on the read surface: unclaimed `PENDING_APPROVAL`/`APPROVED` imports whose `expiresAt` has elapsed are reported as `EXPIRED` immediately instead of remaining visually actionable until a later approve/claim attempt;
- reject approve/claim/revoke transitions after natural expiry without repeatedly mutating revision state on failed requests;
- when a replacement enrollment reuses the same project/name after TTL expiry, durably materialize the old import as `EXPIRED`, invalidate its one-time credential digest, emit audit/outbox authority, and release the unique active-name fence atomically with the successful replacement creation;
- add owner regressions for truthful expired reads, denied expired-token claim/revoke, same-name replacement, emitted expiry audit, and FileStore restart durability of the materialized expiry/credential invalidation;
- keep release regressions version-neutral rather than coupling agent fixtures to the release number.

## 0.0.164 — Cluster enrollment recovery lifecycle closure

- add an explicit revoke lifecycle for unclaimed cluster enrollment requests in `PENDING_APPROVAL` or `APPROVED`, fenced by project write access, optimistic revision matching and `X-Confirm-Revoke: revoke-cluster-import`;
- invalidate the enrollment token digest when an import is revoked, emit audit/outbox authority, reject any subsequent claim, and immediately release the active cluster name for a clean replacement enrollment;
- expose `Cancel enrollment` in the Operator Console to recover safely from lost one-time manifest/token responses without waiting for TTL expiry, while preserving independent `platform-admin` approval for enrollment approval;
- add owner regressions proving revoked tokens cannot claim and the same project/name can be enrolled again after cancellation.

## 0.0.163 — Operator Console effective scope authority closure

- publish backend-authoritative `effectiveOrganizationRoles` and `effectiveProjectRoles` in `/api/v1/access/context`, preserving OIDC direct-role override semantics, durable membership inheritance, global viewer caps and project-scoped API-token organization read-only boundaries;
- make the Console permission surface consume those effective Organization/Project roles instead of treating a global `platform-operator` role as write authority everywhere; fail closed when scoped permission authority is unavailable and recover immediately when a later authoritative Workspace load succeeds;
- bound access-context computation to the principal's accessible organizations/projects and reuse the membership snapshot instead of scanning every tenant/project or repeating the same membership query;
- align workflow-specific UI authority with backend semantics: tenant approval now requires independent `platform-admin`, project operators can cancel maintenance windows where the API permits project write, Marketplace advisory creation is project-write fenced, and dead-letter retry inherits the source event's project/organization write scope;
- close a platform-wide Git authorization gap by requiring `platform-admin` for managed repository creation and signed desired-state revision publication, matching credential/provider, pull-request and rollback authority;
- classify Blueprint release comparison as a pure read-only POST end to end so project viewers can compare releases without a false operator requirement, while durable advisory/Git POSTs remain mutating;
- extend existing backend and Browser owner regressions for effective scoped roles, project-scoped API tokens, direct project overrides, Git operator denial, read-only POST classification and scoped permission transitions.

## 0.0.162 — Operator Console viewer-safe action parity closure

- fix a UI/auth contract mismatch where the read-only permission surface disabled `Create installation plan` even though `/api/v1/installations/plans` is a backend-authorized pure planning POST for `platform-viewer`;
- keep successful runtime-closure `Verify evidence` available to viewers because `/api/v1/runtime-closure-reports/verify` verifies immutable evidence integrity and does not mutate durable authority;
- make viewer-safe control/form metadata authoritative in the Console mutation classifier, so explicit non-mutating POST workflows are not fenced merely because they use a submit button or `*-action` dataset;
- extend the existing owner Browser permission regression and static Console contract to prove mutating controls stay disabled while viewer-safe planning/verification controls remain enabled;
- document viewer-safe POST surface parity as part of the canonical permission-state contract.

## 0.0.161 — Operator Console bidirectional permission-surface closure

- restore mutation controls that were disabled solely by read-only access when a live session authority refresh promotes the operator, instead of leaving the page visually read-only until a full rerender;
- preserve pre-existing contextual disabled state/title when access mode temporarily fences a control, so promotion restores only the permission-owned disablement rather than blindly enabling controls that are unavailable for another reason;
- invalidate and close an already-open destructive confirmation when live session authority demotes the user to read-only, preventing a stale confirmation surface from remaining actionable until backend rejection;
- extend the owner Browser permission-transition regression to prove viewer disablement, operator promotion re-enablement, backend-authoritative demotion, and destructive-dialog invalidation without a full page reload;
- make bidirectional permission-surface restoration and destructive-confirmation invalidation canonical requirements in `DESIGN.md`.

## 0.0.160 — Operator Console session and feedback authority closure

- re-synchronize effective session roles before blocking a read-only mutation, on manual refresh/tab resume, and after backend authorization denial so OIDC group mapping changes cannot leave stale enabled/disabled controls in an open console;
- keep error feedback persistent until explicit dismissal while routine success feedback remains transient;
- replace runtime-generated raw close glyphs in Console and Installer toasts with the canonical SVG action icon;
- extend owner UI quality gates to verify runtime toast action-icon consistency and feedback durability;
- add Browser regression coverage for platform-viewer → platform-operator promotion and platform-operator → platform-viewer demotion without a full page reload.

## 0.0.159 — Operator Console focus and UI quality-gates closure

- Close a real keyboard-accessibility defect in semantic Data Workspaces: `.data-table-shell` referenced an undefined focus token, so the scrollable table region could receive keyboard focus with no visible outline. Introduce canonical light/dark `--focus-ring` and `--focus-halo` tokens and bind global controls, fields and Data Workspace focus treatment to those semantic tokens.
- Replace raw Unicode menu/close action glyphs in the Operator Console and Bootstrap Installer with the existing single-stroke SVG control language. Status text/glyphs remain semantic indicators rather than being converted into decorative iconography.
- Add a stable owner-level rendered UI quality gate, adapted selectively from public UX/UI agent verification practices without importing an external agent harness, design-system library, runtime dependency or aesthetic preset. The gate validates theme references, 280/414 responsive probes, route-level RTL overflow, rendered-DOM names/labels/dialogs/target sizes, keyboard focus trapping, state-aware representative contrast, Data Workspace state behavior and anti-generic action-icon/copy rules.
- Extend `DESIGN.md` so semantic focus tokens, 280/414 narrow-width probes, route-level RTL verification, rendered-DOM accessibility checks, state-aware contrast, Data Workspace state continuity and anti-slop action-icon rules are canonical product requirements.
- Wire the new stable quality gate into the existing `smoke-ui` owner target instead of creating a release-numbered validator or importing the external repository wholesale.

## 0.0.158 — Operator Console cancellation and authority-truth closure

- Treat superseded page-load cancellation as cancellation, not a degradable API failure. `softApi` now propagates `AbortError`, so an aborted route can no longer inject false Partial/Unavailable sources into the successor page or continue rendering fallback state after navigation moved on.
- Fence navigation completion after `loadPage`: a superseded route no longer announces itself as loaded, steals focus, or scrolls the successor workspace after its request was aborted.
- Make Overview summary/readiness authority-aware. Unavailable cluster/baseline/workflow sources render Unknown/Unavailable rather than fabricated zero counts, unknown readiness cannot expose a misleading Continue action, and attention/recent-activity surfaces disclose partial authority instead of claiming there is nothing to act on.
- Invalidate the entire Overview action/summary surface on a hard required-authority failure. Attention, activity and the next-action control can no longer remain stale while only metrics/checklist show an error.
- Extend the existing owner Browser smoke and static Console contract tests to prove abort propagation, superseded-navigation fencing, Overview partial-authority truth and hard-failure stale-state invalidation.
- Canonicalize the accumulated changelog by removing the duplicated embedded second changelog document instead of carrying parallel release truth forward.

## 0.0.157 — Operator Console data-context and permission-fidelity closure

- Preserve the operator's client-side table ordering across authoritative page refreshes and auto-refresh cycles. Sort preference is bound to a stable data-workspace key, reapplied after rerender, and discarded if the table schema no longer supports the saved column instead of silently sorting the wrong data.
- Preserve an active loaded-record filter across a temporary collection-level authority outage so recovery returns the operator to the same working context. Genuine successful small/empty datasets still clear inapplicable filters rather than carrying stale criteria forward indefinitely.
- Centralize semantic table ordering in a reusable owner helper so timestamp/numeric/text comparison, stable ties, missing-value placement and `aria-sort` state remain identical for direct interaction and restored state.
- Close read-only permission drift for runtime-generated Inspector actions. `Requeue dead letter` and future explicit mutation controls inserted into the shared detail dialog now receive the same access-mode enforcement at render time instead of appearing executable until the API rejects them.
- Extend existing Console owner/browser regression coverage to prove sort persistence after rerender, filter-context survival through Unavailable→Recovered authority, and dynamic Inspector mutation disabling for viewer sessions.

## 0.0.156 — Operator Console partial-authority and table-ordering closure

- Separate collection-level **Unavailable** from genuine **Empty** state for all semantic data workspaces. A failed degradable source can no longer render `No records` merely because `softApi` returned an empty fallback; the affected section now names the unavailable authority and offers a page retry while healthy sections remain usable.
- Keep page-level partial-data aggregation while making section truth local and explicit, so operators can distinguish `nothing exists` from `the source could not be read` without interpreting a remote banner. Authentication/session expiry remains non-degradable and still fails at page authority.
- Add accessible client-side ordering for the currently loaded bounded table rows only. Sortable headers expose `aria-sort`, text uses locale-aware natural ordering, numeric values use numeric comparison, and timestamps sort by their raw authority value rather than localized display text. No server-side ordering or pagination authority is fabricated.
- Keep missing values last and equal-value ordering stable in both ascending and descending order so repeated sorts do not create unnecessary row churn.
- Extend the existing owner Browser smoke and Console contract suite to prove numeric ordering, sort direction semantics, filtered loaded rows, read-only mutation intent, and Unavailable-not-Empty rendering.

## 0.0.155 — Operator Console data workspace and read-only authority closure

- Replaced card/list rendering with semantic enterprise data tables for high-cardinality comparable collections: connected/provider clusters, provider profiles, tenant environments, durable operations, audit/security audit, notification destinations/routes/events/deliveries, governed catalog releases and shipped component inventory.
- Preserved all existing row-scoped inspect/approve/retry/recover/revoke/delete and support-bundle actions while adding explicit column hierarchy, numeric/timestamp alignment and internally scrollable narrow-screen behavior.
- Extended the loaded-collection filter so it filters semantic table rows without pretending to provide server-side search or pagination authority.
- Hardened read-only sessions so non-`*-action` mutations such as safe operation cancellation, membership/OIDC revocation, organization editing and Git LKG rollback are disabled before interaction instead of relying on a later API 403.
- Added owner contracts and browser checks for semantic tables, filtered table rows and read-only mutation intent.

## 0.0.154 — Operator Console contextual workflow navigation closure

- Rebuild the Operator Console information architecture around six operator domains — Home, Infrastructure, Delivery, Fleet, Operations and Administration — with contextual second-level destinations instead of exposing every feature as an equal global sidebar item. Preserve every existing route and backend capability while removing backend/package-shaped navigation.
- Move Catalog release governance into Delivery, runtime verification/closure into Fleet Assurance, and organization/tenant/service administration into Administration. Keep Blueprint planning tools available from Blueprint Expert Tools rather than promoting a planning/debug surface to primary navigation.
- Recompose resource-management pages around inventory and status with compact on-demand mutation commands. Cluster connection, maintenance, provider provisioning, assurance execution, fleet configuration, tenant creation, notification routing, catalog signing/release creation and baseline deployment no longer occupy permanent form walls.
- Make Organizations & projects administration list/authority-first: current workspace records lead, while organization/project creation, access grants, OIDC mapping and automation identity creation are explicit contextual actions.
- Add a contextual section navigation band that preserves deep links, route-specific titles, mobile accessibility and keyboard behavior while reducing global navigation load from sixteen destinations to six domains.
- Update the canonical DESIGN.md with the domain/context navigation model, resource-first page anatomy, placement rules and form disclosure contract. The redesign borrows proven interaction patterns from mature infrastructure consoles without cloning their visual identity or importing a new frontend framework.
- Harden owner UI tests for the new information architecture and preserve all existing API, dirty-state, abort/retry, accessibility, responsive and live-authority behavior.

## 0.0.153 — Operator Console final design-system closure

- Establish `DESIGN.md` as the canonical design language for the Operator Console and Bootstrap Installer, and bind contributor guidance to it. The system defines product-specific information architecture, workflow composition, typography, spacing, semantic color, density, status, form, navigation, dialog/drawer, responsive and accessibility rules plus explicit anti-generic patterns.
- Recompose the Operator Console around the operator mental model instead of backend package structure. Preserve all 16 destinations while grouping them into Platform, Infrastructure, Delivery, Operate and System with shorter task-oriented labels and quieter navigation chrome.
- Replace the generic dashboard hero/KPI-card treatment with a runtime-authoritative operator briefing and instrumentation-style status strip. Convert comparable resource collections into dense record/list presentation and reserve cards for genuinely self-contained objects or mutation workspaces.
- Turn Blueprint authoring into a four-stage workflow (Basics, Runtime & delivery, Policy & governance, Scope & publish) with stage-aware validation and safe dirty-state handling. Preserve strict JSON/API parity and overlay capabilities under explicit Expert authoring tools progressive disclosure.
- Make Platform Services observe-first: Git delivery/LKG state leads the page, while provider credential setup, repository creation and signed publishing remain available behind explicit contextual disclosures. Add bounded filtering for large already-loaded operational collections without inventing global/backend search semantics.
- Harden collection filtering across dynamic re-renders, locale changes and small-result transitions; filtered-empty state is recreated from the current DOM rather than retaining stale detached nodes.
- Apply the same final visual language to the Bootstrap Installer: replace the marketing-like bootstrap hero and repeated KPI cards with an installation authority briefing/status strip, flatten installation/health/recovery sections into workflow structure, preserve numbered installer sequencing, and keep cards only for true action workspaces.
- Close final responsive/accessibility findings: mobile Blueprint stages reflow 2×2 instead of leaking document width, expert disclosures use a bounded semantic summary grid, narrow Installer top-bar actions reflow without page overflow, and neutral checklist glyphs meet text contrast.
- Make the browser owner smoke resilient to semantically equivalent HTML attribute ordering when inlining local assets; keep parity/Expert Tools testing on the actual interaction path rather than weakening the product disclosure. Remove temporary pre-design UI copies from the embedded static tree.

## 0.0.152 - UI production hardening closure

- Closed mobile off-canvas focus leaks by making closed navigation inert and hidden from assistive browsing while preserving focus trap, Escape close, and focus return.
- Completed semantic light/dark color tokens for success, warning, danger, neutral, interactive and technical text; Bootstrap Installer now follows system dark mode as well.
- Hardened 320px-1440px reflow with intrinsic-size guards and long technical identifier wrapping without hiding resource authority.
- Added route announcements, document-title updates, named dialogs, aria-busy loading semantics, persistent dismissible error toasts, and 44px interaction targets.
- Added abortable generation-fenced Product Console page loads so stale navigation responses cannot overwrite the current route.
- Replaced Installer overlapping interval polling with visibility-aware, non-overlapping request coordination; repeated transport failures now demote stale Connected state.
- Reduced control-plane polling pressure by making active-work refresh route-local and widening active/idle refresh windows.
- Moved Runtime Certification implementation detail behind progressive disclosure while keeping operator outcome/action prominent.
- Expanded owner UI validation to cover route-by-route reflow and closed-drawer keyboard isolation across production viewport sizes.

## 0.0.151 — Operator Console UI Skills Redesign

- Redesign the production Operator Console around a task/workflow-first interface hierarchy inspired by the selected UI Skills principles: quieter navigation chrome, clearer active state, consistent inline SVG iconography, stronger page/section hierarchy, denser operational lists, purposeful status treatments and fewer decorative visual effects. The redesign reuses the existing vanilla HTML/CSS/JS runtime and adds no UI framework or external runtime dependency.
- Replace the marketing-style gradient overview hero with an operator briefing surface, reduce equal-weight card treatment, normalize panel/resource/form density, make resource details more scan-friendly, and use progressive disclosure for technical detail while preserving every existing authoritative API journey and DOM contract.
- Establish shared accessibility/interaction primitives in the Product Console: 44px primary touch targets, visible focus treatment, skip navigation, logical-property RTL styling, reduced-motion support, high-contrast status semantics, responsive mobile drawer behavior, and consistent control sizing across forms and actions.
- Redesign the Bootstrap Installer with the same production design language while keeping its numbered workflow navigation. Add an explicit mobile navigation scrim, `aria-current`/`aria-expanded` state, Escape-to-close and keyboard focus trapping so the bootstrap workflow remains usable without pointer input.
- Extend the existing Console owner suites rather than adding release-numbered validators. Regression coverage now protects the shared design/accessibility primitives, SVG navigation consistency, responsive/reduced-motion contracts and Installer mobile-navigation semantics. Headless desktop/mobile RTL/LTR console and installer smoke remains authoritative for rendered UI behavior; no synthetic product data was added.

## 0.0.150 — Recovery Authority Identity Binding Closure

- Bind lifecycle destructive-restore recovery to the exact failed restore authority: service, installed profile and backup ID must match. A different backup can no longer bypass a `recoveryRequired` fence or clear an unresolved restore merely because it targets the same service.
- Bind whole-appliance DR recovery to the exact failed backup/profile/object-storage data authority. Endpoint, bucket, prefix, provider and region are immutable across recovery; credential references may rotate without changing backup identity, so operators can recover after credential rotation without redirecting the restore to different data.
- Continue DR recovery from the failed run's durable `CompletedComponents` prefix and original restore-target identities instead of re-capturing authority or replaying components already committed before the failure. Successful recovery clears only blockers with the same exact data authority.
- Validate destructive recovery state fail-closed on startup: reject non-prefix DR restore progress, incomplete recovery target authority and conflicting outstanding recovery authorities instead of selecting the most recent ambiguous blocker.
- Surface backup/profile authority directly in installer run rows so `RECOVERY REQUIRED` has an operator-visible exact restore reference. Add owner regressions for different-backup rejection, object-storage drift rejection, credential rotation, durable progress continuation and conflicting recovery-state rejection.

## 0.0.149 — Destructive Restore Recovery Fencing Closure

- Persist a durable `recoveryRequired` fence when a lifecycle restore or state-aware upgrade recovery fails after destructive restore has begun and the service may intentionally remain quiesced. A terminal `FAILED` run no longer silently releases safety authority and allow an unrelated backup/upgrade to bring partial restored state online.
- Make lifecycle admission recovery-aware per service. Failed generic restore can be remediated only by an explicitly confirmed restore; failed upgrade-recovery can be continued only for the same source upgrade authority. Successful remediation clears the matching durable blockers atomically with terminal success.
- Apply the same invariant to whole-appliance Disaster Recovery. A partial DR restore that remains quiesced is now blocking across process restart, prevents new DR backup/bootstrap/lifecycle mutation, and permits only another explicit restore until a successful restore clears the fence.
- Surface `RECOVERY REQUIRED` in the installer run UI and make malformed durable recovery markers fail closed on load instead of being silently ignored. `recoveryRequired` is valid only on failed destructive restore/recovery runs with the required source authority.
- Add owner regressions proving recovery fences survive restart, reject unrelated mutations, allow the intended remediation path, clear only after successful recovery, and reject corrupted recovery-marker authority.

## 0.0.148 — Upgrade Recovery Admission + Retry Fencing Closure

- Make upgrade-recovery admission atomic with source authority revalidation and acquisition of the per-service lifecycle mutation slot. Runtime preparation may occur before the lock, but the failed source must still be `RECOVERY_REQUIRED` in the same critical section that durably queues the recovery; a concurrently completed recovery can no longer release the active slot and admit a stale second destructive run.
- Preserve the highest destructive-recovery boundary across explicit retry runs. If any exact prior recovery attempt durably reached `RESTORE_COMPLETED`, a new confirmation-bound retry starts from `RESTORE_COMPLETED` and can only restore the previous image/resume the workload; if `RESUME_VERIFIED` was durable, the retry is read-only verification/finalization. The bound backup is never rewound a second time after either boundary.
- Keep explicit retry from `RESTORE_PENDING` available when no durable evidence proves the restore completed, including legacy interrupted recovery attempts. This retains the intentional operator-confirmed recovery path while separating it from post-restore continuation.
- Fail closed when prior recovery history for the same source upgrade disagrees on service, profile, backup ID, previous digest or requested digest, or contains an unsupported durable phase. Corrupt/ambiguous recovery history can no longer be ignored in favor of a fresh destructive restore.
- Add deterministic owner regressions for the source-close admission race, `RESTORE_COMPLETED` continuation, `RESUME_VERIFIED` read-only retry and mismatched prior recovery authority.

## 0.0.147 — Upgrade Recovery Crash Consistency Closure

- Make explicit state-aware upgrade recovery itself crash-consistent with durable `RESTORE_PENDING`, `RESTORE_COMPLETED` and `RESUME_VERIFIED` phases. The destructive backup rewind is durably committed while service writers are still quiesced, so a process crash or final-state persistence failure after resume can never replay the same backup over newer writes.
- Reconcile post-restore recovery only from the already-restored state: re-apply/verify the previous digest and exact replica count without invoking the restore job again. Once resume verification is durable, reconciliation is read-only and fails closed on image/replica drift rather than mutating a completed recovery.
- Atomically close the source failed upgrade as `RECOVERY_COMPLETED` when the recovery run reaches durable success, so the installer console cannot offer the same destructive restore again. Successful 0.0.146 recovery history is repaired on load and duplicate recovery admission is rejected even if legacy source state was left stale.
- Fail closed for interrupted 0.0.146 recovery runs that have no durable recovery phase, because their crash point is ambiguous and automatically replaying a destructive restore cannot be proven safe. A fresh confirmation-bound recovery remains the explicit operator path.
- Add owner regressions proving post-restore replay does not require or touch the backup payload and that ambiguous legacy recovery performs no Kubernetes inspection or mutation.

## 0.0.146 — Lifecycle State-Aware Upgrade Recovery Closure

- Stop automatic image-only rollback for Forgejo, Zot and Keycloak upgrade failures. These managed services own persistent state, so returning only the previous container image cannot prove database/PVC compatibility and may create an old-binary/new-state combination. Rollout or post-rollout image verification failure now persists `RECOVERY_REQUIRED` with the exact pre-upgrade backup ID and previous digest, then fails closed without another automatic mutation.
- Convert legacy `ROLLBACK_PENDING` upgrade runs from 0.0.145-and-earlier to `RECOVERY_REQUIRED` on reconciliation instead of replaying the old unsafe image-only rollback path. Legacy `ROLLBACK_COMPLETED` state is retained for history but no longer described as proof of persistent-state recovery.
- Add a confirmation-bound `POST /api/v1/lifecycle/upgrade-recovery` workflow. Recovery is accepted only for an exact failed `RECOVERY_REQUIRED` upgrade run and copies the source run ID, backup ID, requested digest and previous digest into the new durable recovery run.
- Execute explicit recovery in state-aware order: validate the exact backup, quiesce all service writers, restore the backup while the workload remains stopped, apply the previous digest with UID/resourceVersion fencing, resume the profile's exact replica count, wait for rollout and verify the previous digest. Restore/image/resume failures leave the service fail-closed instead of claiming successful recovery.
- Expose recovery in the installer console only for failed `RECOVERY_REQUIRED` upgrade runs. The confirmation dialog states that persistent-state rewind is destructive and that writes newer than the pre-upgrade backup can be lost; the server independently requires `X-Confirm-Upgrade-Recovery: recover:<upgradeRunId>`.
- Route post-rollout image verification failures into the same durable recovery-required authority. Previously this path could leave the failed/requested image active while the lifecycle run became terminal `FAILED` without rollback/recovery authority.

## 0.0.145 — Migration Session Authority + v27 Mixed-Version Closure

- Hold one PostgreSQL advisory session authority on the dedicated migration connection from compatibility admission through all migration transactions and final schema-history verification, eliminating the runtime check/apply interleave window between concurrently starting 0.0.145+ replicas.
- Explicitly unlock the session authority before returning the connection to the pool; if unlock fails, mark the physical connection bad so a pooled session cannot strand migration authority.
- Reclassify migration 0027 (`owner_destructive_recovery_authority_runtime`) as `QUIESCED_REQUIRED`: its new CHECK constraints are enforced for new writes and legacy writers do not populate `destructive_operation_id` before destructive state transitions.
- Preserve exact, version-bound quiesced approvals; upgrades crossing both historical unsafe boundaries require approvals for both migration 21 and migration 27.
- Keep historical migration bytes/checksums unchanged.

## 0.0.144 — Migration Rollback Schema Authority Closure

- Fail closed when the PostgreSQL `schema_migrations` authority is newer than the migrations embedded in the current `platform-api` binary. A downgraded/rollback binary can no longer silently start on a future schema that it does not understand; startup reports the applied/known migration boundary and refuses the rollback.
- Require every non-empty `schema_migrations` history to be an exact contiguous prefix of the embedded migration sequence before pending schema mutation. Gaps, extra/invalid history rows and future versions are treated as authority corruption/recovery conditions rather than attempting to apply an older missing migration after later schema changes.
- Move the runtime rollback/version fence and mixed-version admission under the same PostgreSQL advisory migration authority used by schema mutation. `platform-api` now calls `ApplyMigrationsWithCompatibility(...)` as the canonical runtime entry point, eliminating the prior check-then-apply race between concurrently starting binaries.
- Preserve Fresh Install and all historical migration bytes/checksums. The existing 0.0.142 live-lock fail-fast policy and 0.0.143 version-bound quiesced migration admission remain unchanged.
- Add owner regressions for future-schema rollback rejection, gapped migration-history rejection and a direct runtime-entry test proving future-schema admission is evaluated after acquiring the migration advisory lock and before any migration DDL.
- This closes a proven rollback/schema-authority defect but does not claim arbitrary binary rollback after data-model changes, Zero-Downtime Upgrade, PostgreSQL Physical Runtime or Exact-SHA Physical PASS. Rollback across future incompatible application/data semantics still requires explicit recovery/restore authority.

## 0.0.143 — Migration Mixed-Version Compatibility Closure

- Add an explicit mixed-version compatibility contract to embedded PostgreSQL migrations. Migration `0021_blueprint_overlay_field_ownership_runtime.sql` is classified `QUIESCED_REQUIRED` because it makes new Blueprint revision columns `NOT NULL` without defaults; an older API writer that does not know those columns can therefore fail immediately after that schema commit. Existing rolling upgrades now detect that pending boundary before applying any pending schema mutation instead of falsely treating arbitrary historical DDL as N/N+1 compatible.
- Keep Fresh Install zero-touch: a database without an existing `schema_migrations` authority, or with an empty migration history, may still apply the complete embedded schema automatically because no old application writer exists. Existing databases default to `rolling` admission.
- Make the exceptional quiesced path explicit and version-bound. `PLATFORM_FACTORY_POSTGRES_MIGRATION_MODE=quiesced` alone is insufficient; `PLATFORM_FACTORY_POSTGRES_QUIESCED_MIGRATIONS` must list the exact pending migration version (for example `21`). Leaving a stale quiesced configuration behind therefore cannot silently authorize a different future incompatible migration.
- Require every future embedded migration to receive an explicit mixed-version compatibility classification before the migration package can load it. This prevents a newly added migration from silently inheriting `rolling-safe` semantics by default.
- Preserve all historical migration bytes/checksums and the 0.0.142 advisory-lock/lock-timeout ownership model. This release does not retrofit migration 21 into an online expand/contract migration and therefore does not claim zero-downtime for upgrades that cross that historical boundary; it converts a previously unsafe rolling path into a truthful fail-closed/quiesced requirement.
- High-Load, Zero-Downtime Upgrade and Exact-SHA Physical Runtime PASS remain unclaimed. Physical PostgreSQL mixed-version tests under live traffic are still required before certifying upgrade continuity.

## 0.0.142 — Migration Live-Lock Fail-Fast Closure

- Harden PostgreSQL schema migration under live traffic without weakening the existing cross-replica migration advisory authority. A new replica may still wait for the dedicated migration advisory lock while another new replica owns the migration lane, but once it owns that lane every migration transaction now applies PostgreSQL-local `lock_timeout=5000ms` and `statement_timeout=300000ms` before touching schema state.
- Prevent rolling upgrade DDL from waiting indefinitely behind long-lived transactions in the currently serving release. If the required schema lock cannot be acquired within five seconds, the new replica fails startup/rollout rather than holding a queued DDL lock that can amplify into a prolonged control-plane outage. This is deliberately fail-fast availability protection, not a claim that arbitrary historical DDL is online/expand-contract safe.
- Keep migration integrity semantics unchanged: migration bytes/checksums remain untouched, the transaction-scoped advisory lock remains canonical, schema changes and `schema_migrations` recording remain in one owner transaction, and the existing ten-minute startup migration budget still governs legitimate waiting for another new replica to finish its lane.
- Use explicit PostgreSQL millisecond GUC values rather than Go duration syntax and fail closed if the transaction-local migration safety settings cannot be applied. Add owner regressions proving the safety settings are established after advisory ownership and that configuration failure rolls the migration transaction back.
- High-Load, Zero-Downtime Upgrade and Exact-SHA Physical Runtime PASS remain unclaimed. This release bounds one proven live-upgrade lock-amplification failure mode; full expand/contract migration discipline, real PostgreSQL contention/failover testing and Physical upgrade-under-traffic evidence remain required.

## 0.0.141 — Security Audit Backpressure & Shutdown Drain Closure

- Bound the per-replica PostgreSQL Security Audit admission queue at 4,096 pending events so a slow/unavailable database or hostile request burst cannot grow the in-memory audit backlog without limit and OOM the API process. Queue saturation fails closed before a protected request can continue.
- Preserve canonical micro-batched hash-chain semantics while hardening durable DB failure behavior: after the serializable transaction exhausts its own retry budget, the currently admitted backlog is rejected with the same audit failure instead of keeping thousands of request goroutines pinned behind a persistently unavailable database. New requests may form a fresh batch and observe recovery.
- Add an explicit `DrainSecurityAudit` shutdown boundary. Public and agent HTTP listeners now stop admission concurrently, then the process waits for already-admitted/cancel-retained Security Audit work before deferred PostgreSQL close. Generated API workloads raise `terminationGracePeriodSeconds` to 75 seconds to cover the 45-second HTTP drain plus the bounded audit-drain phase.
- Make authentication/authorization DENY paths surface Security Audit persistence failure as `503 SECURITY_AUDIT_UNAVAILABLE` instead of returning a normal 401/403 while silently losing forensic evidence. Access remains denied; the response now truthfully identifies the unavailable audit authority. Session logout remains best-effort audited because blocking a privilege-reducing logout on audit availability would be a security regression.
- Add owner regressions for bounded queue admission, fail-closed backlog rejection after durable PostgreSQL failure, graceful drain of admitted events, DENY-path audit backpressure, and the extended generated termination budget. The global canonical audit chain remains intentionally serialized across replicas; this release improves overload/restart correctness but does not claim High-Load or Exact-SHA Physical Runtime certification without real PostgreSQL load/termination testing.

## 0.0.140 — Security Audit Batch-Chain Throughput Closure

- Preserve the existing fail-closed, globally ordered tamper-evident Security Audit chain while removing one-advisory-lock-per-event amplification from the PostgreSQL hot path. Concurrent audit appends on each `platform-api` replica are now micro-batched (bounded at 64 events) and committed under one serializable transaction, one global advisory-lock acquisition and one tail-sequence lookup.
- Allocate every event sequence and `previousDigest` inside the locked transaction and compute each digest from the immediately preceding committed candidate, so batching changes throughput cost rather than trust semantics. Cross-replica batches remain serialized by the same PostgreSQL advisory authority and therefore cannot fork the canonical hash chain.
- Keep request handling fail-closed: callers do not receive a successful audit result until their batch transaction commits. If the HTTP/request context is cancelled, the already-observed authentication/authorization decision intentionally remains queued for immutable audit persistence while that caller receives its context error. Batch execution itself has a bounded 15-second database budget.
- Replace the previous per-replica one-at-a-time admission-lane regression with an owner test proving two audit events share one transaction/one advisory lock, receive monotonic sequences, chain `previousDigest` correctly, and pass canonical `ValidateSecurityAuditChain`. This retains the 0.0.139 pool-convoy protection while amortizing the global lock cost across concurrent events.
- This release does **not** claim that the global Security Audit serialization ceiling has disappeared: batches from different replicas still serialize on the canonical chain head. Partitioned/signed checkpoint chains remain a possible future architecture only if Physical load evidence demonstrates the single canonical chain is insufficient. High-Load, Zero-Downtime Upgrade and Exact-SHA Physical Runtime PASS are not claimed without real load/soak/failover execution.

## 0.0.139 — High-Load Auth, Pool & Runtime Budget Closure

- Remove the PostgreSQL OIDC group-mapping full-table scan from the authentication hot path. Runtime group resolution now normalizes/deduplicates the groups asserted by the current identity and queries only matching `ACTIVE` mappings through the existing group/state index; an identity with no asserted groups performs no mapping query at all.
- Contain Security Audit lock convoys without weakening the existing fail-closed global tamper-evident chain: each `platform-api` replica now admits at most one in-flight PostgreSQL security-audit append, so burst traffic cannot consume that replica's entire DB connection pool while many requests wait behind the cross-replica advisory chain lock. Waiting for the local audit lane is context-cancellable. The global chain remains serialized and is still an explicit High-Load architecture limitation rather than being falsely declared solved.
- Make PostgreSQL connection budgets explicit and fail-closed through `PLATFORM_FACTORY_POSTGRES_MAX_OPEN_CONNS` / `PLATFORM_FACTORY_POSTGRES_MAX_IDLE_CONNS`, with validated bounds and an idle<=open invariant. Bootstrap profiles set a bounded API pool budget instead of implicitly allowing every HA replica to consume the prior default independently.
- Harden generated `platform-api` workloads for termination/load pressure: both bootstrap profiles now carry explicit CPU/memory requests and limits, a 60-second termination grace period, startup and liveness probes in addition to readiness, while the production HA profile explicitly enforces rolling `maxUnavailable: 0`, `maxSurge: 1` and `minReadySeconds: 10`. These are runtime safety defaults, not a substitute for Physical load certification or HPA capacity evidence.
- Fix a real generated-manifest defect in the single-node bootstrap path where the Catalog signing key environment entry emitted a duplicated `secretKeyRef:` mapping. Stable bootstrap regressions now cover the secret binding plus the high-load runtime settings across both single-node and HA manifests.
- Add owner regressions proving OIDC resolution no longer scans all active mappings, empty group assertions avoid PostgreSQL entirely, Security Audit admission waits do not take a DB connection, pool settings reject malformed/oversubscribed configuration, and generated HA rolling availability settings remain present.
- This release does **not** claim High-Load, Zero-Downtime Upgrade, PostgreSQL Physical Runtime or Exact-SHA Physical PASS. The global Security Audit chain is intentionally still a serialization point; service/DR scale-to-zero backup, online expand/contract schema evolution, component-specific certification, Production upgrade certification and real load/soak/failover campaigns remain open.

## 0.0.138 — High-Load Summary, Scope & Agent Jitter Closure

- Remove the PostgreSQL canonical `Snapshot()` from the polled Control Plane summary hot path. Runtime summary counts are now computed by bounded, scope-aware SQL aggregates and never materialize append-only step/audit history or sealed operation evidence payload bytes merely to render dashboard cardinalities; development Memory/File stores retain the canonical snapshot fallback.
- Add a dedicated global aggregate fast path for platform-wide operators and a scoped visible-resource aggregate path for tenant/project views, preserving authorization semantics while avoiding full object hydration.
- Add a bounded PostgreSQL operations collection path and make the Operator Console request only the 200 most recently updated operations. Legacy API callers that omit `limit` retain the existing full-list contract; explicit limits are validated and capped at 500.
- Remove global Project-table scans from project-scoped API-token authorization and from direct OIDC Project-role resolution. Project-bound credentials resolve their immutable Project authority directly, while organization-scoped users enumerate only Projects in organizations they can read.
- Spread Platform Agent polling with deterministic per-cluster jitter: startup contact is distributed over the first 20% of the normal interval and recurring cycles vary within ±10%, reducing synchronized Fleet bursts after mass enrollment/restart without weakening task fencing, certificate rotation or inventory semantics.
- Add stable owner regressions proving the runtime summary path does not fall back to canonical Snapshot materialization, bounded operation reads use the paged store path, oversized requested limits fail closed, and project-scoped API tokens do not trigger a global Project scan.
- This release reduces concrete high-load amplification paths but does **not** claim High-Load, Large-Fleet, Zero-Downtime Upgrade or Exact-SHA Physical Runtime certification. The global synchronous Security Audit hash-chain serialization, online/expand-contract migration discipline, service/DR scale-to-zero backup behavior, component-specific certification and real Physical load/failover campaigns remain open work.

## 0.0.137 — HA Notification, Graceful Shutdown & Migration Atomicity Closure

- Give every notification dispatcher replica a unique lease identity; an unexpired delivery/outbox lease can no longer be reclaimed merely because the caller presents the same worker name.
- Raise notification delivery fencing beyond the maximum webhook timeout and process a bounded batch concurrently, preventing serial batch work from expiring later claims while preserving throughput.
- Add a durable PostgreSQL fleet-health scan lease (with development-store equivalent) so only one API replica performs each fleet-wide notification health scan interval.
- Make platform-api shutdown wait for graceful HTTP drain completion with a budget greater than the server write timeout, so rolling termination cannot let main exit before in-flight drain finishes.
- Preserve historical migration bytes/checksums while making `ApplyMigrations()` the sole runtime owner of the PostgreSQL transaction and advisory lock: legacy files with one outer `BEGIN/COMMIT` wrapper are normalized only at execution time, unbalanced/nested transaction control fails closed, and already-applied migration checksums remain upgrade-compatible.
- Add migration `0044_notification_health_scan_ha_lease.sql` for HA notification scanner coordination.
- This release does not claim high-load, zero-downtime upgrade, PostgreSQL physical runtime, or Exact-SHA Physical PASS; the larger audit/security-audit/DR/summary scaling findings remain separate work.

## 0.0.136 — Component Runtime Authority & Planner Scope Closure

- close a critical certification scope over-claim: `TARGET_RUNTIME_V1` installs the dedicated `secure-namespace-foundation` harness and proves target-cluster DNS/TLS/network/isolation/storage/backup/observability capabilities, but 0.0.135 could reuse that same catalog-wide evidence digest as `target-runtime-certified` metadata for arbitrary components that were never installed from their immutable Catalog source; RUNTIME admission now authorizes the directly installed foundation component only and fails closed for every other component until a component-specific runtime certification authority actually installs and verifies that component;
- close a cross-organization Catalog authority injection boundary: certification evidence used for channel promotion is now owned by the Catalog release's organization regardless of `PLATFORM`/`PRIVATE` visibility, so a consumer organization may run local certification of a shared RENDER but cannot use its Project evidence to authorize another organization's RUNTIME publication; authority validity no longer changes merely because the current viewer can or cannot read the evidence Project;
- close an offline/source-metadata execution-readiness bypass: `plan.Build` and therefore `platformctl plan`, raw `/api/v1/plans`, and Blueprint flows without an authoritative Catalog release can no longer become `execution-ready` solely because component files contain exact versions, source locks and digest-shaped certification metadata. A separate authority-aware planner entry point is used only after the control plane has revalidated a bound RUNTIME/PRODUCTION Catalog release;
- propagate that authority distinction through Blueprint create/update/clone/resolve-preview, keeping unbound/static Catalog planning advisory while allowing an actually admitted authoritative Catalog release to provide execution readiness; preserve the existing 85 blockers for the shipped Enterprise Blueprint today because its current source/pinning/certification blockers already prevent execution, rather than adding a redundant authority blocker before those are closed;
- strengthen negative controls: a valid managed image mirror can no longer mask missing component-specific runtime certification, the Catalog governance owner test proves catalog-wide TARGET_RUNTIME evidence does not certify `gateway-api`, and a foreign-organization TARGET_RUNTIME run cannot authorize a platform-visible Catalog owned by another organization. Operator Console wording now explicitly distinguishes target-capability/foundation evidence from component runtime certification. Exact-SHA Physical Runtime PASS is not claimed without direct execution of this final artifact.

## 0.0.135 — Catalog Certification Authority Integrity Closure

- close a critical Catalog governance authority bypass: RUNTIME/PRODUCTION admission previously treated component certification metadata plus any digest-shaped `sha256:...` value as sufficient evidence, allowing a manually supplied hash to satisfy the control-plane publish path without a durable certification authority record; RUNTIME admission now requires evidence backed by an active `TARGET_RUNTIME_V1` run bound to the exact published RENDER source release/revision, source-lock set, organization/project scope and canonical profile;
- revalidate certification evidence at the point of use instead of trusting a persisted `SUCCEEDED` label: the control plane reconstructs the install checkpoint and final evidence digest from the retained PASS checks, validates the required INSTALL/VERIFY check shape, requires an unexpired/non-revoked run, re-renders the immutable RENDER source to match the recorded render/source-lock context, requires current cluster inventory/environment fingerprint and required target capabilities to still match, and rejects evidence when the source signing trust is no longer active;
- prevent stale/legacy false authority after upgrade: a previously published RUNTIME release with unsupported certification metadata can no longer be rendered, used as a Runtime Certification source, or bound into a Blueprint merely because its old signature is valid; successful certification reports also fail closed if their retained evidence no longer reconstructs correctly;
- keep PRODUCTION fail-closed: no durable upgrade-certification authority exists in the current product, so `upgrade-certified` metadata is not accepted merely because it carries a digest. This release deliberately does not fabricate that missing authority; Production promotion remains blocked until supported-upgrade evidence has a real owner/store/workflow;
- replace the image-mirror runtime smoke's former `SMOKE_TEST_ONLY`/fixed-hash certification fixture with a real local control-plane TARGET_RUNTIME run over an imported cluster inventory and complete INSTALL/VERIFY result contract before testing RUNTIME mirror admission/rewrite. The catalog-governance smoke retains a fixed hash only as a negative control and proves it is rejected. Exact-SHA Physical Runtime PASS is not claimed without execution of this final artifact.

## 0.0.134 — Helm Render Provenance & Live Interaction Closure

- close a real external Helm supply-chain reproducibility gap: an immutable `render-manifest.json` was digest-bound, but the source lock/provenance did not identify the exact Helm/Crane toolchain and render-affecting inputs that produced it; Helm catalog bundles now require one strict `render-generation` contract and fail closed when it is absent, malformed or inconsistent;
- bind the Helm version, Crane version, fixed release name, component namespace, `--include-crds` semantics, exact Kubernetes min/max render versions and every product-owned values file path+SHA-256 into both `source-lock.json` and `provenance.json`; bundle verification requires canonical equality of those generation records and runtime catalog verification independently enforces the same Helm generation semantics;
- prevent ephemeral local values from entering executable catalog authority: acquisition accepts only values files whose resolved targets stay inside the repository, records their repository-relative paths and exact hashes, rejects duplicates, and repository validation re-hashes the retained files before a resolved Helm component is accepted;
- close a separate live-console interaction race exposed by the full UI authority smoke: Baseline polling could replace a resource card while an operator was expanding nested read-only `<details>`, detaching the clicked element and collapsing inspection state; automatic refresh now defers during recent pointer/keyboard interaction and while an expanded detail exists in the active page, with a headless negative control plus the previously failing live journey proving the boundary;
- strengthen stable owner/negative controls for missing generation evidence, namespace/Kubernetes-window mismatch, values provenance, runtime verification, external catalog import and live interaction preservation without creating a release-numbered validator. Real acquisition of the remaining unresolved upstream components is not claimed in this environment because Helm/Crane acquisition tooling cannot be fetched here; existing Enterprise Blueprint deployment blockers therefore remain authoritative until real upstream acquisition and runtime certification are executed. Exact-SHA Physical Runtime PASS is not claimed without execution of this final artifact.

## 0.0.133 — Panel RBAC, Dirty Scope & Maintenance Race Closure

- close a real read-only authorization parity defect between the Operator Console and backend middleware: the planning-only Blueprint authoring round-trip, Blueprint resolve preview and compatibility evaluator POST endpoints are now explicitly viewer-safe in the server RBAC authority as well as the browser guard, so a `platform-viewer` no longer sees enabled planning controls that fail with HTTP 403 at runtime; mutating Blueprint/catalog actions remain operator/admin-only;
- close unsaved-state loss in the Blueprint authoring journey: importing strict Blueprint JSON into the visual editor now marks the visual form dirty, reset/edit/clone/lifecycle transitions cannot silently replace dirty Blueprint state, and the shared dirty helper correctly clears a dirty root form in addition to descendants;
- close silent editor replacement in notification administration: opening another persisted Destination or Route, or cancelling an edit, now detects unsaved values and requires explicit discard before authoritative data replaces the form; successfully loaded/reset editors are marked clean deterministically;
- close a cross-cluster Cluster Maintenance scope defect where selecting a cluster without a maintenance profile could retain the previous cluster's environment and drain timeout, creating a path to save stale settings into the wrong cluster; authority refresh now resets clean forms to safe defaults for a profile-less cluster while preserving each independently dirty maintenance form;
- fence asynchronous maintenance authority loads with a generation token so a slow response for a previously selected cluster cannot arrive after a newer selection and overwrite the current cluster/profile/window/run state; add runtime headless negative controls for dirty timeout preservation, profile-less cluster reset and stale-response ordering;
- extend stable owner regressions for the viewer-safe planning endpoint set, Blueprint import dirty semantics, maintenance scope reset/race fencing and the notification/Blueprint discard guards. This release does not alter or fabricate upstream acquisition/runtime-certification evidence: the enterprise Blueprint remains planning-only until its real deployment blockers are closed, and Exact-SHA Physical Runtime PASS is not claimed without execution of this final artifact.

## 0.0.132 — Panel Session, Dirty-State, Compatibility & Diagnostics Closure

- close the Operator Console authentication fault-isolation defect: API 401 responses now preserve structured status/body information, bypass `softApi` degradation, and enter one global session-expiry path for both page reads and direct mutations, so an expired session cannot silently render authoritative resources as empty/partial data; ordinary read failures remain independently degradable;
- replace focus-only refresh suppression with real unsaved-change protection: active forms and the standalone Blueprint JSON editors become dirty on user edits, adaptive polling remains paused after focus leaves the control, navigation/manual refresh/language switching and browser unload guard against discarding edits, and successful form submissions/reset/canonical Blueprint import/export clear dirty state deterministically;
- make Marketplace and Tenant workflows live participants in adaptive polling and active-work detection, covering provisioning/resizing/suspending/resuming/deleting/approval-wait states instead of requiring operator refresh while durable work progresses;
- complete two existing backend-to-panel capability gaps without adding a parallel engine: Operation cards can download the authoritative `operation-diagnostics` support-bundle profile, and Blueprint Tools can execute `/api/v1/compatibility/evaluate` against a concrete Kubernetes/architecture/distribution/provider target and render the server-reconstructed compatibility authority, including blocker responses; the compatibility action remains available to read-only viewers because it is planning-only;
- close the remaining mobile drawer keyboard boundary with a Tab/Shift+Tab focus trap and focus restoration on Escape/scrim close, on top of the existing scrim, scroll lock and ARIA state;
- extend stable owner/headless regressions for 401 propagation through `softApi`, dirty-form persistence after blur, standalone Blueprint editor dirty state, Marketplace/Tenant live refresh, compatibility authority rendering/viewer access and mobile drawer focus trapping. The enterprise Blueprint remains planning-only while its real upstream source/runtime-certification blockers exist; this release does not fabricate acquisition or certification evidence and does not claim Exact-SHA Physical Runtime PASS.

## 0.0.131 — Panel Operator Readiness, Live State & Integration Closure

- make the main Operator Console operationally live without overwriting active edits: operational pages now use adaptive refresh (4 seconds while durable work is active, 15 seconds otherwise), pause while a form/dialog is being used or the tab is hidden, and resume on visibility; this closes stale RUNNING/UPGRADING/VERIFYING/notification state that previously required manual refresh while keeping the installer and main console semantics aligned;
- isolate read-path failures at the page data-source boundary with `softApi` and a visible degraded-data banner, so a temporary audit/catalog/notification/fleet endpoint failure no longer makes unrelated healthy widgets disappear; authoritative mutations remain fail-closed and are never softened;
- close the main Git onboarding feature gap by exposing the existing durable `/git-credentials` and `/git-providers` authority in System Services: platform administrators can create environment-backed credential references, connect the supported Forgejo provider, inspect bindings, rebind an active credential, rotate and revoke without pasting raw secrets into the product database or browser state;
- complete notification edit journeys by allowing persisted Destination and Route records to be loaded back into their authoritative forms and updated with exact `If-Match` revision fencing; conflict responses now explain that the resource changed and require a refresh rather than presenting an opaque 409/412;
- improve operator session and mobile accessibility semantics: API 401 now surfaces session expiry and returns the user to sign-in, primary navigation publishes `aria-current`, the mobile drawer has `aria-expanded`/scrim/Escape close/body scroll lock, and a keyboard skip-link reaches the main console;
- extend the stable webconsole owner contract to require the Git provisioning routes and the new live/fault-isolation/edit/session contracts. This release improves Panel usability only; it does not claim Exact-SHA Physical Runtime PASS without direct execution evidence for this final ZIP SHA.

## 0.0.130 — Release Integrity & Certifier Process Lifecycle Closure

- close a release-builder source-boundary defect where repository symlinks were followed by `Path.is_file()`/`shutil.copy2`, allowing content outside the repository tree to be silently copied into the release ZIP; repository validation and release packaging now reject symlinks and other non-regular source entries before reading or copying them;
- make Exact-SHA manifest literals canonical across Go and Python consumers: the Go release inspector no longer trims whitespace around `sha256` or `mode`, and accepts only lowercase 64-hex SHA-256 plus canonical `0oNNN` mode strings; an Artifact cannot now PASS Physical inspection while failing the canonical release verifier solely because one consumer normalizes manifest bytes;
- require canonical Unix ZIP creator metadata in the Go Exact-SHA inspector, matching the release builder/verifier extraction contract instead of accepting DOS/FAT metadata with platform-dependent permission interpretation;
- close a PostgreSQL runtime-certifier process-lifecycle gap discovered by the full regression matrix: partial worker-launch failure now terminates every worker that was already started before failing closed, while the timeout regression asserts process termination directly instead of depending on scheduler-sensitive PID-file side effects;
- close an installer completion-publication race where the durable bootstrap journal could already report `SUCCEEDED` while the server's in-memory `bootstrapActive` flag had not yet been cleared, causing an immediate lifecycle/backup request to receive a transient `MUTATION_CONFLICT`; terminal durable state now overrides that stale publication lag while failed runs still block non-resume mutations;
- add stable owner regressions for whitespace-padded manifest digest/mode, non-Unix ZIP metadata, external source-tree symlink packaging and partial concurrent-worker launch failure. The four release layers remain independent; this release does not claim Exact-SHA Physical Runtime PASS without direct execution evidence for this final ZIP SHA.

## 0.0.129 — Canonical Release Identity & Path Closure

- bind the release manifest identity to the actual packaged `RELEASE-NAME` bytes and canonical ZIP root (`4so-platform-factory-<version>-<releaseName>`), require the release-name slug contract, and reject archives missing `RELEASE-NAME`; a non-empty self-claimed `manifest.releaseName` can no longer stand in for packaged release identity;
- reject non-canonical archive and manifest paths rather than silently normalizing `.`/`..`, repeated separators, backslashes or surrounding whitespace into trusted paths; this aligns Exact-SHA inspection, release verification and archive extraction on one unambiguous file identity model;
- reject duplicate JSON object keys in the release manifest across Go and Python consumers, eliminating parser last-wins ambiguity at the self-describing supply-chain boundary;
- close a Python path-resolution bypass in the advertised non-symlink Exact-SHA boundary: release verifier, Autopilot physical preflight and compatibility bundle builder now preserve the caller path through `lstat`/`O_NOFOLLOW` instead of resolving a symlink to its target before the check;
- harden `verify_release.py` to validate the full manifest identity/schema, canonical root, `VERSION`/`RELEASE-NAME` parity, and generated provenance/SBOM release identity instead of accepting content-hash-valid metadata with contradictory product/release labels;
- give Codex Autopilot exact-release preflight and the compatibility appliance-bundle builder the same canonical identity rules, with the compatibility builder additionally snapshotting the opened release before hashing/inspection so its `sourceReleaseDigest` cannot be split across separate mutable-file opens;
- add stable owner negative controls for missing/mismatched `RELEASE-NAME`, forged manifest release identity, non-canonical ZIP paths and Autopilot identity mismatch. The four release layers remain independent and this release does not claim Exact-SHA Physical Runtime PASS without direct execution evidence for this final ZIP SHA.

## 0.0.128 — Exact Artifact Four-Layer Binding & Snapshot Closure

- close the remaining in-place release-artifact TOCTOU after the 0.0.127 pathname/inode fix: Exact-SHA inspection now copies the already-opened release file once into an unlinked mode-0600 private snapshot while hashing it, and both outer digest authority and all ZIP/manifest/content verification operate exclusively on those copied bytes; same-inode overwrite can no longer make the accepted SHA describe bytes different from the files that were inspected;
- give the Python release verifier the same single-snapshot semantics instead of separately opening the archive for executable-mode inspection, extraction and final SHA calculation; the verifier now creates one private snapshot with no-follow/opened-inode checks and reports the digest of the exact bytes it extracted and verified;
- close a four-layer Autopilot binding defect where extracted-artifact Full Verification could run against the locally built `root/release/...zip` while Physical Field Campaign consumed a different `PLATFORM_FACTORY_AUTOPILOT_RELEASE_ARTIFACT`: real-test preflight now validates the physical artifact identity/version/installer bytes first, and `artifact-full-verify` runs directly against that exact same path before any Physical PASS can be considered;
- harden Autopilot's own Exact-SHA preflight against split-open/self-claim behavior by snapshotting the release once, hashing the snapshot and verifying the actual installer entry bytes against the manifest in that same snapshot; stable negative controls cover forged installer self-claims, private-snapshot stability and exact physical-artifact stage selection; this release still does not claim Exact-SHA Physical Runtime PASS without executing this final SHA on a supported physical/systemd runtime.

## 0.0.127 — Release Artifact Content Integrity & TOCTOU Closure

- Hardened Exact-SHA release inspection so `ARTIFACT-MANIFEST.json` is no longer treated as an authoritative self-claim: every manifested archive file is streaming-hashed and compared with its declared SHA-256 before any embedded binary digest can be trusted.
- Reject release ZIPs with missing, unmanifested, duplicate, size-mismatched, mode-mismatched, or digest-mismatched files, and require the canonical manifest schema/product/version/release identity contract.
- Closed the release-artifact path replacement TOCTOU by binding outer ZIP SHA calculation and inner archive inspection to the same opened inode/file descriptor (`Lstat`/`f.Stat` identity check + `zip.NewReader` on that handle).
- Hardened `verify_release.py` against duplicate archive paths and duplicate manifest paths so release verification and Exact-SHA Physical inspection share the same archive-identity semantics.
- Added owner-level negative controls proving a forged installer digest in the manifest and duplicate archive entries are rejected before Exact-SHA runtime binding can succeed.

## 0.0.126 — Installer Runtime Identity & Autopilot Physical Gate Closure

- close the remaining generated-runtime identity gap in Exact-SHA certification: Field Evidence schema v3 now records the SHA-256 of the actual running `platform-installer` executable derived from `/proc/self/exe`, Field Campaign schema v3 binds that digest alongside the exact release archive digest, and prepare/start/resume fail closed before mutation unless the live installer's `/api/v1/access/status` identity equals `bin/linux-amd64/platform-installer` in the exact release artifact manifest; schema-v1/v2 state stays recovery-readable but cannot satisfy the current Physical gate;
- close an Autopilot real-test consumer defect introduced by the stronger Exact-SHA collection contract: real-test now resolves and validates `PLATFORM_FACTORY_AUTOPILOT_RELEASE_ARTIFACT` before execution, always performs a fresh schema-v3 evidence collection with `--release-artifact` even when local campaign state previously says `evidenceVerified=true`, independently verifies the resulting report against the same ZIP, and refuses `REAL_TEST_PASS` unless release and running-installer digests still match after collection; stale mutable local state can no longer substitute for fresh runtime evidence;
- make artifact verification independently prove the running installer identity: release archive inspection parses the canonical artifact manifest and exposes per-file digests, `field-evidence verify-report --release-artifact` compares the report's running-installer digest with the installer binary pinned inside that exact ZIP, and stable negative controls reject a replaced/wrong installer before Start/Resume mutation as well as schema-v2 campaigns for the new Physical gate;
- repair generated/runtime contract drift by updating Field Campaign and Installer Access Status schemas for the current runtime contracts, and surface product version, exact installer binary digest, exact release digest and running installer digest in the bootstrap UI; preserve the four independent release layers and do not infer Exact-SHA Physical Runtime PASS from these source/generated/negative-control results.

## 0.0.125 — Field Campaign Exact Evidence Chain Closure

- close a remaining Exact-SHA campaign/evidence substitution gap at collection time: independently verified Field Evidence now exposes its accepted `specDigest`, `bundleDigest` and `preflightDigest`, and `field-campaign collect` requires all three to equal the prepared campaign in addition to the existing run identity/state and `releaseArtifactDigest`; a report from a different appliance bundle assembled from the same release ZIP and same install request can therefore no longer attach to an older campaign merely because the deterministic run ID and release SHA happen to match;
- re-prove the exact release artifact at the evidence-collection boundary for schema-v2 campaigns: `field-campaign collect` now requires `--release-artifact`, independently inspects/hashes that ZIP again and refuses collection when its SHA differs from the campaign binding; schema-v1 state remains readable/collectable for recovery compatibility but remains ineligible for the Exact-SHA Physical gate;
- add stable owner regressions for same-run/same-release/different-bundle evidence substitution, no-mutation on rejected evidence, missing collection-time release artifact and mismatched exact ZIP rejection; keep the four release layers independent and do not claim Exact-SHA Physical Runtime PASS without executing this final artifact SHA on the supported physical/systemd runtime.

## 0.0.124 — Exact-SHA Physical Evidence Binding Closure

- bind every newly built appliance bundle to one exact 4SO Platform Factory release archive without creating a self-referential archive hash: the bundle build specification carries `metadata.sourceReleaseDigest`, `platformctl appliance-bundle build` independently hashes and validates the supplied `--release-artifact` ZIP/version, and the sealed `bundle.json` plus `bundle.lock.json` retain that source release digest under the existing manifest/lock integrity chain; legacy bundles remain readable for recovery, but new certification workflows require the binding;
- upgrade the Field Campaign contract to schema v2 so prepare records `releaseArtifactDigest`, rejects an Installer bundle whose source-release binding differs from the exact local release ZIP, rechecks the same binding before Start, and requires independently verified field evidence to carry the identical release digest before collection succeeds; schema-v1 campaign state remains verifiable for recovery compatibility but is not eligible for the new Exact-SHA physical gate;
- upgrade field evidence to schema v2 by cryptographically including `releaseArtifactDigest` in the canonical evidence inputs and verification result, while preserving strict verification of legacy schema-v1 reports; `platformctl field-evidence verify-report` now independently hashes `--release-artifact` and refuses to verify a v2 evidence report against any other release ZIP, so an auditor can directly establish Release ZIP SHA → sealed bundle → installation run → field evidence;
- close the Autopilot real-test inference gap: `--real-test` now requires `PLATFORM_FACTORY_AUTOPILOT_RELEASE_ARTIFACT`, requires a schema-v2 campaign whose `releaseArtifactDigest` equals the SHA-256 of that exact file both before destructive execution and before `REAL_TEST_PASS`, and therefore cannot elevate legacy/non-bound runtime evidence into an Exact-SHA Physical PASS; stable owner regressions cover archive identity/version inspection, required release binding, evidence tampering, legacy recovery compatibility and Autopilot exact-SHA preflight without creating a release-numbered validator;
- preserve the four independent release layers: this release adds the missing cryptographic binding needed by the fourth layer, but does not itself claim Exact-SHA Physical Runtime PASS until this exact final ZIP SHA is actually executed on a real supported physical/systemd runtime and the bound evidence is collected.

## 0.0.123 — Canonical Snapshot & Restore Atomicity Closure

- make authoritative Snapshot representation deterministic across MemoryStore, FileStore and PostgreSQL instead of inheriting backend/query/map iteration order: the shared canonicalizer now normalizes OIDC mappings, compensation steps, agent certificates, Git credentials/providers and all existing canonical resource families, and the PostgreSQL Snapshot path applies the same canonical ordering before returning; this prevents semantically identical state from producing backend-dependent or run-dependent serialized/checksummed representations;
- close a restore failure-atomicity defect in the development durable authority: `MemoryStore.Restore` now validates the immutable security-audit hash chain before acquiring/replacing live state, so a corrupted/invalid snapshot returns an error without first clearing the currently valid organizations/resources; this also preserves FileStore rollback semantics if restore is ever asked to reject invalid input;
- add stable owner regressions for canonical ordering of previously map-backed unordered families and for invalid-security-audit restore preserving pre-existing authority; no release-numbered validator, feature expansion or synthetic physical evidence is introduced;
- keep the four-layer release gate independent: Source Semantics, Generated/Installed Runtime Semantics and Runtime-Realism Negative Controls may PASS independently, but Exact-SHA Physical Runtime remains unclaimed unless this exact artifact SHA is executed on a real physical/systemd runtime with direct evidence.

## 0.0.122 — PostgreSQL Snapshot Runtime Scope & Readiness Closure

- close production PostgreSQL Snapshot parity gaps that left newer control-plane resource families invisible to runtime consumers even though Memory/File stores exposed them: notification destinations/routes/events/deliveries/attempts, agent certificates, recovery checkpoints, Git credential/provider/PR/managed-revision authority, cluster-import credential digests, entitlements/OEM profiles, tenants and provider profiles/clusters are now populated from authoritative PostgreSQL read paths with deterministic ordering; notification health therefore sees certificate expiry state and scope-index consumers see the same resource families in production as in development;
- make canonical PostgreSQL audit Snapshot history complete and correctly ordered: security-audit rows are read without the user-facing 1000-row cap in ascending sequence order and validated as a complete hash chain, while normal audit Snapshot rows are read unbounded in canonical chronological order; API-facing `ListAudit`/`ListSecurityAudit` remain bounded latest-first, so presentation pagination is not confused with canonical runtime state;
- preserve tenant audit/support completeness under heavy foreign activity: project support timelines now scope the canonical audit history before applying their 1000-event response cap, and project-scoped notification route writes authorize the actual project rather than incorrectly requiring organization-write authority; moving a project route across projects requires access to the destination project and expanding it to organization-wide scope still requires organization-level write authority;
- remove canonical-Snapshot amplification from high-frequency health paths: `/readyz` uses a constant-size PostgreSQL aggregate query for organization/operation counts (with a store-compatible fallback) instead of materializing the complete Snapshot, and the notification health scanner reads only projects, managed clusters, latest inventories and agent certificates; stable regressions prove both paths remain operational even when canonical Snapshot loading is deliberately unavailable;
- keep release evidence layered: source/runtime/negative-control gates may validate these fixes, but no Physical PASS is inferred without executing this exact release SHA on a real physical/systemd runtime and retaining direct evidence from that SHA.

## 0.0.121 — Audit Scope & Notification Project Isolation Closure

- close scoped audit completeness drift introduced as the control-plane model grew: the canonical audit visibility index now includes project-/organization-owned Blueprint overlays/releases, organization catalog governance resources, operation traces/compensation steps, notification destinations/routes/events/deliveries/attempts, cluster maintenance resources, agent certificates, runtime certification runs, recovery checkpoints and scoped OIDC mappings; lifecycle events whose later audit records omit explicit organization/project metadata are therefore still visible to their rightful tenant without exposing foreign resource ids;
- apply audit pagination only after authorization scope for non-platform-admin callers: `GET /api/v1/audit-events?limit=N` now derives the caller scope from the authoritative Snapshot, filters all visible audit rows, then selects the newest N, preventing unrelated newer tenant activity from starving a caller's own audit trail; platform-admin keeps the bounded direct store path;
- close project-scope bypass in notification reads: project-scoped API tokens and direct project-role identities can no longer list or directly fetch notification events, routes or deliveries owned by a different project in the same organization; project-bound notification resources now authorize through `requireProjectAccess`, while organization-owned resources continue to use organization authority;
- apply notification event/delivery limits after caller scope whenever a global list is requested, while explicit organization/project queries continue to filter in the store before limiting; this removes cross-tenant pagination starvation without weakening sort order, delivery state filters or organization-wide operator behavior; stable negative controls prove foreign project route/event/delivery denial, own-project visibility, tenant-safe limits and the expanded audit index.

## 0.0.120 — Notification Secret, Egress & Daemon CLI Authority Closure

- close organization-admin process-secret exfiltration through notification destinations: credential references are no longer allowed to name arbitrary Manager environment variables, notification secrets must live under a dedicated organization-ID-bound `PLATFORM_FACTORY_NOTIFICATION_SECRET_<ORG_ID>_*` namespace, credential-bearing destination writes additionally require platform-admin authority, and the dispatcher independently rejects legacy/persisted rows that violate the runtime namespace before any environment lookup or network request;
- close server-side notification webhook access to loopback/link-local/metadata-class targets: the production dispatcher resolves the target itself, rejects loopback, link-local, multicast, unspecified and IPv4 0/8 addresses before dialing, dials the validated numeric address to prevent DNS-rebinding between validation and connect, and keeps redirects disabled; private RFC1918/ULA targets remain supported for intentional disconnected/internal notification endpoints;
- make platform daemon CLI parsing fail closed before initialization: `platform-api`, `platform-agent` and `platform-probe` now expose side-effect-free help/version actions and reject unknown/multiple arguments with exit 2 instead of silently starting network/state mutation on operator typos; stable owner regressions cover the parser and notification credential/egress boundaries, and the live notification smoke proves organization-bound credentials, dispatch/retry history and restart persistence;
- remove the remaining fresh-artifact race-verifier stall mode: canonical `--full` verification still derives the exact package set from `go list ./...` and runs every package with `-race -count=1`, but isolates each package in its own invocation so a slow/high-concurrency package cannot stall unrelated coverage; this changes scheduling only, not the package set or race assertions;
- keep the release defect-driven: no target feature, parallel workflow engine, release-numbered validator, synthetic runtime evidence or project-state document was introduced; Git, installer, lifecycle, upgrade, tenant, provider, fleet and UI behavior remain on their existing owners.

## 0.0.119 — Installer Host Authority & Runtime Safety Closure

- close live systemd false-positive admission across local bootstrap, HA peer and host-deployment preflight: a present `systemctl` binary is no longer sufficient; live execution requires the systemd manager to be running/reachable, with `/run/systemd/system` present on the local host, so container/chroot/offline-systemd hosts fail before mutation;
- close fresh-host Kubernetes residue blind spots by evaluating the real systemd unit search path (with a complete fail-safe fallback including `/usr/local/lib/systemd/system`, generated/transient/attached paths) and resolving known conflicting units through `systemctl cat`; RKE2/K3s/kubelet residue in a valid systemd load path can no longer be admitted as fresh;
- fence bootstrap evidence and HA trust inputs against active/interrupted execution races: public preflight cannot overwrite evidence owned by an in-flight or interrupted run, SSH private-key/known-hosts mutation is blocked while bootstrap execution owns the host, interrupted runs still permit credential repair before Resume, and conflict conditions map to stable HTTP 409 semantics instead of generic input/server failures;
- make installer CLI introspection side-effect free: `--help`, `-h`, `help`, `--version` and `version` are parsed before transport/token/state initialization, while unknown arguments fail with exit 2 before filesystem or network mutation; extend stable owner and HTTP simulation coverage for these contracts, align shipped examples/README binary paths with the release layout, and make full release race verification execute the exact `go list ./...` package set in bounded fresh-test batches so the canonical verifier completes reliably without reducing race coverage.

## 0.0.118 — Git PR Canonical Bundle & Base Authority Closure

- close a signed desired-state authority gap before any Git mutation: revision publication now requires exactly the six product-owned revision files, requires the detached signature file, binds the revision id deterministically to the signed spec digest, and rejects rendered managed-state, revision-marker or kustomization content that is not the canonical product rendering of the signed descriptor; incomplete/tampered bundles can no longer create staging side effects or become a recorded managed authority;
- bring pull-request delivery to the same base-authority boundary as direct delivery: before creating a durable PR intent or staging branch, `main` must equal the latest durable ManagedGitRevision and exact managed tree, or—when no prior authority exists—the exact product bootstrap seed; unmanaged content already present on the base therefore cannot be inherited invisibly through a base-to-head allowlist;
- make PR candidate and post-merge validation independently exact-tree authoritative in addition to signed-descriptor, immutable-head and changed-file fencing, and apply the same canonical-bundle validation to last-known-good restore input before mutation; extend stable Git owner/smoke coverage for incomplete-bundle no-side-effect rejection, unsigned revision-id rejection and unmanaged-base PR rejection without adding release-numbered validators or parallel project-state documents.

## 0.0.117 — Git Direct/LKG Branch CAS Authority Closure

- close the remaining direct-delivery and last-known-good Git branch race without depending on a Gitea-only branch-ref update API: DIRECT_COMMIT and LKG restore now stage the exact managed file set on a deterministic isolated branch, bind it to the exact expected base commit, validate the complete base-to-stage changed-file allowlist, then advance the managed branch through a Forgejo-native internal pull request using `fast-forward-only` plus the exact `head_commit_id`; concurrent target movement or staging-head replacement fails closed, while a lost merge response is reconciled only when the target already equals the exact staged commit;
- strengthen normal managed pull-request merge with the same immutable remote head fence by sending the persisted approved head through Forgejo `head_commit_id`, so a head change between pre-merge validation and the merge request is rejected by the Git authority rather than detected only after mutation; keep post-merge complete-diff validation and atomic local PR/ManagedGitRevision commit as a second independent boundary;
- prevent historical or concurrent unmanaged repository content from becoming managed authority: every reconciled/direct/latest/LKG commit that is trusted as product-owned must expose exactly the canonical `README.md` plus the six product-managed revision files through the repository Contents API, with no unexpected file, directory, symlink or submodule; this closes upgrade safety for authorities recorded by older releases that could have inherited unmanaged content;
- make LKG rollback exact-tree authoritative: matching signed revision metadata alone no longer counts as an already-restored response-loss recovery, the live tree must be byte-for-byte equivalent to the confirmed LKG commit; otherwise rollback requires the live branch to match the latest durable ManagedGitRevision and its exact managed tree before mutation, and the post-restore tree must again equal the LKG tree before a new authority is recorded; unmanaged drift therefore cannot be preserved or adopted by rollback;
- close residual Git action availability/compatibility gaps: drift adoption returns `GIT_AUTHORITY_UNAVAILABLE` instead of dereferencing a missing Forgejo integration after a configuration/restart change, deterministic PR/direct/restore staging branch names are sanitized and capped at the API-supported 100-character limit, Forgejo fast-forward-only merge capability is proved/enabled before any staging branch or revision-file mutation so an incompatible API leaves zero partial publication side effects, and stable owner/smoke regressions prove Forgejo-native fast-forward CAS, approved-head merge fencing, pre-existing unmanaged drift rejection, LKG unmanaged-drift rejection, target movement rejection and recovery without any release-numbered validator or synthetic readiness evidence.

## 0.0.116 — Action Authority, Git Reconciliation & Destructive Confirmation Closure

- close Git pull-request authority and crash-recovery gaps end to end: persist a durable REQUESTED PR intent bound to the exact base commit, signed revision/digest/fingerprint and deterministic head branch before any remote mutation; persist the exact candidate head commit after staging, require approval to bind that same immutable head, reject pre-existing/recovered branches or PR history unless the complete base-to-head diff contains only product-managed paths, re-read Forgejo review/head state on retries, and reconcile an already-committed remote approval/merge instead of blindly repeating it;
- make pull-request merge locally atomic and race-safe: verify the approved head immediately before merge, verify the resulting base branch after merge against the persisted pre-merge base and complete managed-file allowlist, then commit the MERGED pull-request state and corresponding ManagedGitRevision in one Memory/File/PostgreSQL authority transaction so crashes cannot leave two local truths; remote-merge crash recovery accepts only the exact already-merged PR and signed revision;
- bind last-known-good rollback to the exact operator-confirmed authority instead of re-resolving a moving LKG: requests now carry the LKG id, revision and immutable commit SHA, server-side confirmation binds that exact target, changed authority is rejected, and a post-mutation retry reconciles an already-restored signed revision before recording a new managed revision rather than blindly restoring again;
- close stale administrative writes for Organization Membership, Entitlement and OEM Profile: creation requires `If-None-Match: *`, update requires the exact `If-Match` revision, and both Memory/File and PostgreSQL stores enforce the same precondition atomically so a stale admin form cannot reactivate revoked membership, restore a superseded entitlement, or overwrite a newer OEM profile;
- make Disaster-Recovery and Lifecycle Restore confirmation server-authoritative rather than browser-only: `X-Confirm-Restore` must bind the exact backup id (and lifecycle service) before the destructive restore owner is entered, while the console sends the same exact confirmation contract;
- close API-token issue/rotate response-loss duplication without persisting raw secret material: require a durable `Idempotency-Key`, persist only that request identity alongside the one-way token digest, enforce per-service-account uniqueness in Memory/File/PostgreSQL, reject committed retries before minting another token, and preserve the key across FileStore restart while redacting it from API/outbox responses; add stable owner/API regressions for stale RBAC/policy writes, PR candidate/merge changed-file authority, historical PR non-adoption, exact LKG confirmation, restore confirmation and token issue/rotate idempotency; no feature, target abstraction, synthetic certification/supply-chain evidence, release-numbered validator or parallel project-state document was added.

## 0.0.115 — PostgreSQL Certification Object Identity & Cleanup Fencing Closure

- close a destructive certification ownership defect left after schema-name isolation: the PostgreSQL runtime certifier now binds the certification schema to the exact PostgreSQL namespace OID immediately after a successful create, injects that OID guard into normal and concurrent certification SQL, and refuses subsequent migration/check/cleanup work if the same schema name resolves to a replacement object; schema identity is evidence-bearing instead of inferred from a random name;
- remove pre-delete behavior from restore-database certification and bind every created restore database to its exact `pg_database.oid`: `createdb` must succeed without deleting a pre-existing same-name database, backup/restore and post-restore checks re-verify the bound database OID, and cleanup refuses to drop a same-name replacement rather than using name-only destructive authority;
- make cleanup ownership explicit and fail closed: a certification schema/database is eligible for cleanup only when this run durably knows that its own create succeeded and has a valid bound OID, so a failed `CREATE SCHEMA` collision can no longer end with `DROP SCHEMA ... CASCADE` against the pre-existing schema and a failed restore-database create can no longer delete an unrelated database; add stable owner regressions for schema-OID guards, concurrent SQL guards, create-collision no-delete, restore-database OID replacement, and zero cleanup of objects not created by the run; no feature, synthetic runtime evidence, supply-chain lock, release-numbered validator or parallel project-state document was added.

## 0.0.114 — PostgreSQL Certification Schema Isolation Closure

- close a destructive physical-certification isolation defect: the PostgreSQL runtime certifier no longer configures `search_path=<certification-schema>,public`, which could let a missing/partially migrated certification relation fall through to production `public` authority tables after a schema/migration failure; runtime certification sessions now expose only the generated certification schema in their explicit search path, so unresolved test relations fail instead of mutating product data;
- prove schema isolation as part of fresh migration execution before and after the migration set, and gate every follow-on authority mutation/contention/restart/backup/restore check on a durably successful isolated migration phase; if schema creation or any migration fails, later destructive checks fail closed without issuing SQL against production authority while cleanup still removes any partial certification schema/artifacts;
- add stable PostgreSQL certifier owner regressions proving there is no `public` fallback, rejecting sessions whose visible search path contains `public` or whose current schema is not the certification schema, and refusing all post-migration authority mutations when isolated migration did not complete; no product feature, distribution abstraction, synthetic runtime evidence, supply-chain lock, release-numbered validator or parallel project-state document was added.

## 0.0.113 — Bootstrap First-Bind Ownership & Restart Fencing Closure

- close the bootstrap control-plane first-bind adoption boundary: `platform-api` and `platform-internal-services` now carry a product bootstrap ownership annotation in both single-node and compact-HA foundation manifests, and every durable bootstrap-object bind rejects an unowned first observation instead of treating the first same-name UID as product authority; durable UID binding still rejects later same-name replacement;
- close the earlier materialization gap before RKE2 static manifests can manufacture apparent ownership on pre-existing objects: the first foundation deployment requires `platform-system` to be absent when no durable target identities exist, successful materialization immediately binds both critical live UIDs to the install run, complete prior identities are re-verified on idempotent replay, and a partial durable identity set fails closed as an uncertain interrupted materialization rather than being adopted or replayed;
- replace the name-only `kubectl rollout restart deployment/platform-api` runtime-persistence probe with an ownership- and identity-fenced JSON Patch that tests the exact Deployment UID/resourceVersion and bootstrap owner marker, writes a unique Pod-template restart annotation, waits for rollout, then re-reads the same durable UID and exact restart postcondition before certification can continue;
- add stable bootstrap owner regressions for unowned first bind, pre-existing namespace rejection, partial materialization identity rejection, safe complete-identity replay, same-name replacement after restart, and single-node/HA ownership markers; stale fake JSON-Patch behavior was corrected without weakening runtime fencing, and no feature, distribution abstraction, synthetic supply-chain/certification evidence, release-numbered validator or parallel project-state document was added.

## 0.0.112 — Upgrade / DR Authority & Workload Postcondition Closure

- make lifecycle upgrade crash recovery phase-authoritative instead of replay-based: persist the pre-upgrade image and explicit apply/rollback phases before external mutation, persist rollback authority before attempting recovery after a failed rollout, reconcile an interrupted rollback only toward the already-authorized previous image, and make a durably completed rollback terminal so a reboot between rollback completion and final FAILED persistence can never re-apply the failed requested image; legacy interrupted upgrades without durable phase authority fail closed rather than inventing replay permission;
- bind disaster-recovery restore of product authority Secrets to durable pre-mutation target identity: before a Restore run is queued, capture the exact namespace/name/UID/resourceVersion plus required type/key/label shape for platform internal credentials, ingress TLS, managed Git credentials and Agent mTLS; restore manifests, installer-authority synchronization and transient Git-signing-key cleanup require those exact UIDs, so a foreign same-name replacement cannot receive backup credentials and legacy interrupted restore runs without the binding cannot be automatically replayed;
- strengthen the shared Kubernetes workload mutation primitive used by Lifecycle and DR: scale/image JSON Patch still tests UID/resourceVersion (plus container/current image for updates), now also validates the API server PATCH response actually contains the requested replicas/image and exposes exact postcondition verification helpers; quiesce/resume and upgrade/rollback re-read those desired replicas/images after pod termination or rollout so a same-UID concurrent drift cannot be certified as success;
- update only the stable owner-suite fakes to model Kubernetes PATCH state transitions and add negative controls for same-UID post-rollout image/replica drift, interrupted rollback non-replay, terminal rollback, durable restore-target capture, foreign Secret replacement and legacy restore rejection; no new product feature, target distribution, synthetic supply-chain lock/evidence, release-numbered validator or parallel project-state document was introduced.

## 0.0.111 — Fresh Install State Authority & Residue Closure

- make fresh management-plane installation fail closed on stopped/partial Kubernetes residue rather than inferring a clean host from a missing RKE2 kubeconfig and free ports: local and HA-peer admission now reject product-owned RKE2 state/binaries/units as well as conflicting K3s, kubelet and kubeadm state/units before the first RKE2 side effect;
- prevent stale 4SO bootstrap authority from being silently inherited by a new installation when the durable bootstrap journal is missing: preflight rejects generated RKE2/PostgreSQL/identity/session/bootstrap credentials, bootstrap PKI/signing material, staged bootstrap bundles, Git handover/object identities, HA state and lifecycle/DR state while preserving legitimate installer-access and operator SSH trust inputs;
- close the configurable-state-root split-brain boundary for real local execution: live LocalSystem bootstrap accepts only the canonical `/var/lib/4so-platform-installer` authority already used by product-owned host credentials/runtime material, while alternate `PLATFORM_INSTALLER_STATE_DIR` roots remain available only to isolated simulation/test adapters; Resume retains its persisted preflight evidence after owned mutation and does not re-run the fresh-host gate;
- add stable owner regressions for stale RKE2 state without kubeconfig, K3s/kubeadm/kubelet residue, generated bootstrap-state reuse and preservation of valid SSH/access inputs; no reset workflow, target feature, synthetic supply-chain lock, physical certification evidence, release-numbered validator or parallel project-state document was introduced.

## 0.0.110 — Bootstrap Credential Durable Revocation & Secrecy Closure

- prevent bootstrap API credentials from leaking into Kubernetes PodSpec/kubectl process arguments during installer-owned in-cluster HTTP probes: dynamic method/endpoint/actor/payload data now travels over stdin, the bootstrap token is resolved only through a Kubernetes `secretKeyRef`, temporary probe Pods disable service-account token automount, run without privilege escalation/capabilities on a read-only root filesystem, and have a hard 120-second active deadline so an interrupted installer cannot leave an unbounded credential-bearing probe;
- make bootstrap credential revocation durable across RKE2 reconciliation and machine reboot: before mutating live objects the installer sanitizes the authoritative `/var/lib/rancher/rke2/server/manifests/4so-platform-foundation.yaml` so neither the Secret data nor the platform-api environment reference can recreate the token, then applies the existing UID/resourceVersion-fenced live revocation, verifies absence on the same object identities, and removes the root-only local bootstrap-token source file;
- fail closed if durable desired-state revocation cannot be persisted: the installer performs no live credential mutation when the authoritative foundation manifest is missing/unusable, preventing a false `revoked` state that a later RKE2 manifest replay could undo; both single-node and compact HA foundation encodings are covered by stable owner regressions;
- keep scope defect-driven: no feature, target-distribution abstraction, synthetic supply-chain lock, physical certification evidence, release-numbered validator or parallel project-state document was introduced; canonical external/runtime blockers remain authoritative.

## 0.0.109 — Bootstrap Crash Recovery & Authority Truth Closure

- close blind replay of interrupted bootstrap side effects after installer process loss or machine reboot: every bootstrap step now has an explicit owner interruption policy, unknown future steps fail closed, and persisted `RUNNING` state can no longer inherit automatic replay authority merely because the worker disappeared;
- make interrupted RKE2 installation recovery verification-only: Resume proves the local kubeconfig and active RKE2 server and, for `production-standard-ha`, the exact three-node Ready management quorum before marking the step recovered; if completion cannot be proven, the journal records `INTERRUPTED_OUTCOME_UNCERTAIN` and the installer shell/HA join is never automatically rerun, while an operator can repair the host and Resume will reconcile the postcondition without incrementing the execution attempt;
- make interrupted initial Git publication authority-reconciled rather than blindly republished: Resume accepts only the exact deterministic revision ID/digest plus a full immutable commit SHA already present in durable local handover evidence or the Control Plane managed-Git revision authority, reconstructs the local handover/Application state from that authority, and refuses automatic republish when the publication outcome is uncertain;
- make executable appliance installation plans truthful about the pre-database authority boundary: evaluation and standard-HA bootstrap plans now expose `bootstrap-authority-gate` backed by the durable local bootstrap journal, spec/bundle digest binding, mutation exclusivity and interruption policy instead of falsely claiming PostgreSQL runtime certification before Kubernetes/PostgreSQL exist; planning-only/existing-authority flows retain the PostgreSQL runtime gate where that authority can actually exist;
- add stable owner regressions proving policy coverage for every bootstrap step, no RKE2 installer replay on uncertain recovery, postcondition-only RKE2 recovery, Git no-republish behavior, local/control-plane Git authority reconciliation and bootstrap-plan authority truth; no supply-chain lock, physical certification evidence, feature, distribution abstraction, release-numbered validator or parallel state document was fabricated.

## 0.0.108 — Physical Certification Truth & Remote Bootstrap Safety Closure

- make remote bootstrap staging operation-isolated and cleanup-authoritative: Prepare/Apply now derive cryptographically unique per-operation stage paths, the staged `platformctl` path is unique per execution rather than shared by binary digest, cleanup is bounded and returned as an operation error instead of being silently discarded, and failed receive/staging paths still attempt removal so concurrent installs cannot delete each other’s control binary or leave sensitive bundle/TLS material behind unnoticed;
- close interrupted-stage residue without cross-operation deletion: remote stage receive uses destination-scoped temporary directories and cleanup removes only the exact operation stage plus its `.previous`/matching `.incoming` family, preserving unrelated concurrent attempts while preventing crash leftovers from retaining bootstrap payloads;
- make PostgreSQL physical restart certification prove a real server transition: capture the live postmaster identity before the configured restart/failover action, require that identity to change before accepting recovery, terminate complete subprocess groups on timeout, and make certification cleanup a mandatory authoritative check so a no-op restart command, orphaned client or retained destructive test schema cannot produce PASS;
- make PostgreSQL TLS certification session-authentic rather than configuration-only: require the profile’s `sslmode=verify-full` contract and verify the current backend in `pg_stat_ssl`, including an active TLS session, protocol and cipher, so `SHOW ssl=on` on a plaintext certification connection can no longer create fake evidence;
- refresh and expiry-bind the PostgreSQL secure-minor baseline to the 2026-08-13 upstream security release (16.15/17.11/18.6), record the authoritative baseline source/publication date, fail closed when that baseline ages beyond its allowed window, and prevent `SOURCE_DATE_EPOCH` from forging wall-clock timestamps in physical runtime evidence while retaining reproducible timestamps for contract-only evidence;
- add stable owner regressions for concurrent remote-stage/control-binary isolation, authoritative cleanup, attempt-scoped residue removal, postmaster-identity restart proof, process-group timeout cleanup, mandatory artifact cleanup, live TLS-session proof, security-baseline expiry and physical-evidence timestamps; no synthetic upstream lock, physical PASS, distribution feature, release-numbered validator or parallel state document was introduced.

## 0.0.107 — Management-Plane Kubernetes Identity Fencing Closure

- eliminate name-only lifecycle and disaster-recovery Job replay/cleanup: backup/restore Jobs now use operation-scoped names and ownership annotations, are created rather than force-applied, persist the exact Kubernetes UID before observation, reject legacy/foreign Jobs without durable identity, and fail closed if a same-name replacement appears instead of deleting or blindly replaying an uncertain side effect;
- make disaster-recovery authority restoration identity-safe and restart-safe: platform/ingress/agent/Git credential Secrets are restored in place with UID+resourceVersion JSON-Patch preconditions rather than delete/create or unfenced merge-patch, and the transient GitOps signing key is removed only after durable component completion with an idempotent identity-fenced cleanup so a crash cannot make resume impossible;
- bind Lifecycle quiesce/resume/upgrade and Disaster Recovery quiesce/resume to durable workload UID authority before the first scale/image mutation; replica and image changes use JSON Patch UID/resourceVersion tests, container identity/current-image tests where applicable, and post-quiesce/post-rollout UID verification so a restarted operation cannot mutate or certify a same-name replacement;
- harden installer bootstrap-credential revocation: remove the name-only `kubectl set env` path, durably bind both `platform-api` Deployment and `platform-internal-services` Secret identities to the install run, remove credentials through UID/resourceVersion-fenced JSON Patch, and re-read/verify the same identities and absence of the credential before marking the step successful;
- add owner regressions for Job replacement/replay, workload replacement during/after mutation, UID/resourceVersion patch fencing, DR Secret restoration/cleanup ordering, and bootstrap credential same-name replacement; no feature, distribution abstraction, release-numbered validator, synthetic certification evidence or documentation state artifact was added.

## 0.0.106 — Runtime Fencing, Persistence & Resume Closure

- close tenant Kubernetes ownership adoption across ResourceQuota, LimitRange, NetworkPolicy and ConfigMap reconciliation: existing objects are now read and ownership-verified before conditional SSA, absent objects use create-or-verify, and a same-name foreign/concurrent object is never force-adopted;
- tighten Baseline PLAN → APPLY → ROLLBACK state binding: approved ADD/UPDATE/rollback operations now reject managed-label-preserving content drift and only treat retries as idempotent when the exact desired/forward state still matches the approved plan, while replacement and resourceVersion conflicts remain fail-closed;
- extend Cluster Maintenance fencing through the actual execution deadline by carrying the durable lease expiry in the task and deriving the Agent execution context from it; an expired/stale Agent cannot continue cordon/drain mutations after lease authority is lost;
- make Runtime Certification cleanup generation mandatory at the Agent boundary and helper signatures: every network/storage/backup ephemeral resource requires a non-empty durable cleanup token, eliminating empty-token side effects that could not be safely recovered after crash/retry;
- enforce Forgejo atomic-publication capability before any repository mutation for both publish and restore, so an incompatible Forgejo fails before EnsureRepository/first desired-state write instead of failing mid-publication; update the existing stable Git LKG smoke fixture to model the admitted Forgejo API version;
- repair PostgreSQL startup and Serializable retry correctness: the production startup parent deadline now actually covers the independent ping plus ten-minute migration budget, and transaction retry paths reset/replace attempt-local result slices and flags so a rolled-back/failed-commit attempt cannot leak duplicate rows, stale replay state or certification checks into the committed result;
- expose installer worker ownership in status and let the existing UI resume a durable RUNNING journal only when no in-memory worker is active, closing the reboot/process-crash dead-end without automatically replaying uncertain side effects;
- remove unused blind-force SSA and name-only raw Kubernetes delete helpers after confirming all live destructive paths use ownership checks plus UID/resourceVersion preconditions; no feature, distribution, validator family or synthetic certification evidence was added, and canonical supply-chain/physical blockers remain fail-closed.

## 0.0.105 — Runtime Network, Safe Maintenance & Forgejo Capability Closure

- make TARGET_RUNTIME network probe cleanup identity a compile-time contract: network server/client helpers now require a non-empty task cleanup token explicitly, both cross-tenant deny and explicit-allow probes carry the real cleanup token, and the stable owner test executes with a non-empty token and asserts every probe Pod is labeled with that authority so the previous Agent/cleanup mismatch cannot be hidden by an empty-token fixture;
- make Cluster Maintenance drain fail safe before destructive mutation: the Agent now preflights the complete current Pod set before the first eviction, refuses unmanaged Pods that lack a `controller=true` owner, refuses Pods using `emptyDir` unless a future explicit ephemeral-data-loss authority is introduced, and still uncordons the node when admission fails; this prevents partial drain and permanent loss of standalone/local-ephemeral workloads;
- bind every maintenance Pod eviction to the exact Kubernetes Pod identity observed during drain by carrying UID/resourceVersion in the Pod view and sending both through `policy/v1` Eviction `deleteOptions.preconditions`; same-name replacement or concurrent mutation returns conflict instead of evicting the replacement, while PDB `429` remains a distinct retryable drain condition;
- fail Forgejo service readiness when the reported server version cannot provide the atomic multi-file repository API required by managed Git publication; accept Forgejo 1.20+ and current major releases, reject older/invalid versions, and make the existing system-service/bootstrap health boundary surface this incompatibility before the first desired-state publish rather than failing later at runtime;
- keep the release scope defect-driven: no new distribution/OKD feature, validator family or synthetic certification evidence is introduced, and the existing canonical release-plan blockers remain fail-closed.

## 0.0.104 — Git Atomic Publication & Commit Authority Closure

- make managed desired-state publication to Forgejo atomic: `PublishRevision` now resolves current file identities and submits all changed files through one repository multi-file commit instead of committing files one-by-one directly to `main`, preventing Argo CD from reconciling a partially published platform revision; idempotent publish produces no extra commit, and Git rollback uses the same atomic publication primitive;
- require full immutable Git commit identity end-to-end wherever a managed/LKG/source commit becomes control-plane authority: only 40-hex SHA-1 or 64-hex SHA-256 commit IDs are accepted by blueprint/control-plane/PostgreSQL delivery and drift state, Forgejo publication/branch responses, and rollback source validation; abbreviated SHAs fail closed;
- bind bootstrap GitOps handover evidence to the exact full commit returned by publication: the Application keeps managed `main` branch tracking so later product-owned revisions are not frozen, but bootstrap records the published full commit and requires Argo `status.sync.revision` to equal that exact commit before handover is accepted;
- separate product release blockers from deployment-context Git identity in Codex Autopilot while still delegating all blocker discovery to the canonical planner: the current default plan remains 85-blocked, with 84 product-owned acquisition/pinning/certification/source-lock blockers and one runtime deployment-context `GIT_REVISION_NOT_IMMUTABLE` blocker that can only be resolved after the real Forgejo publish/observe boundary; no placeholder commit or synthetic release evidence is introduced;
- update stable fake-Forgejo integration/smoke fixtures to model the supported multi-file repository commit endpoint, including exact per-file SHA preconditions for updates, and add regressions proving one revision produces one commit and an unchanged re-publish produces none.

## 0.0.103 — Codex Autopilot Durable Resume & Canonical Release Readiness Closure

- make deterministic Codex/Autopilot stage execution crash-safe: each in-progress run now persists an atomic checkpoint containing the selected stage-graph signature, workspace fingerprint, forward/convergence cursor, repair count and failure fingerprints; an interrupted runner resumes only the proven prefix when the workspace is unchanged, while any source/stage-graph change invalidates the checkpoint and restarts from a safe boundary; completed PASS/FAIL/BLOCKED runs remove the checkpoint so a later intentional test run never reuses stale success;
- make stage timeout/restart process-safe: every canonical stage and Codex repair runs in an isolated process group, timeout terminates the complete group with TERM→KILL escalation, and the checkpoint records the active PID plus Linux process start identity so a subsequent runner invocation can terminate a still-running process group left behind by an externally killed Autopilot before resuming; add owner self-tests for descendant-process termination, valid-prefix resume, stale-workspace reset and stranded-stage recovery;
- replace the Autopilot release-readiness shortcut that inspected only `source.resolved` with the product's canonical `platformctl plan -f blueprints/enterprise-private-cloud.json` authority; `autopilot-release-test`/`autopilot-real-test` now require `executable=true` and zero deployment-plan blockers, while normal local correctness still returns `LOCAL_CODE_PASS` and reports the full plan blocker count/codes without asking Codex to patch external prerequisites;
- verify the current baseline honestly remains planning-only with 85 canonical blockers (18 runtime certification, 18 certification evidence, 16 unresolved source, 16 unpinned versions, 16 missing source locks and 1 non-immutable Git revision); no placeholder commit, fabricated source lock, fake certification evidence or release-numbered validator was added to turn those external/evidence requirements green.

## 0.0.102 — Codex Autopilot, Crash Recovery & Test Readiness Closure

- make Codex Autopilot a first-class bounded test/repair entry point: preflight now checks Go/Make, C compiler, libpq development availability, Python test modules, a usable system or Playwright-managed Chromium, and the actual Codex command executable before a repair run; the default non-interactive repair invocation uses an explicit `workspace-write` sandbox while preserving `PLATFORM_FACTORY_CODEX_COMMAND` override support;
- separate deterministic local-code correctness from external release prerequisites: normal `autopilot-test`/repair runs can report `LOCAL_CODE_PASS` without treating unresolved third-party supply-chain inputs as code defects, while `autopilot-release-test` and `autopilot-real-test` remain fail-closed on unresolved source bundles and live-test prerequisites; document stable Autopilot entry points and add pinned Python test requirements plus Playwright-managed Chromium fallback so a disposable Codex test host does not depend on a preinstalled system browser;
- close the recurring disaster-recovery owner-test flake by waiting for the intentionally asynchronous second backup operation to reach durable success before `testing.TempDir` teardown, preventing a background persistence write from racing directory cleanup while leaving production backup semantics unchanged;
- close TARGET_RUNTIME crash-orphan recovery without unsafe name/label garbage collection: every Runtime Certification task claim now durably records an attempt/phase cleanup generation and cryptographic cleanup token in Memory/PostgreSQL authority before Agent execution; ephemeral network/storage/backup resources carry that token, and a later lease holder cleans deterministic prior VERIFY-attempt resources only after owner+token verification and current UID/resourceVersion fencing; foreign/replacement objects are never deleted;
- add PostgreSQL migration 0042, API/task chaining coverage and direct Agent negative controls proving prior-attempt cleanup succeeds only for the durable token, while keeping unresolved upstream acquisition and physical PostgreSQL/HA/Air-gap/DR certification explicitly external rather than fabricating release evidence.

## 0.0.101 — Runtime Cleanup Failure Semantics Closure

- make Runtime Verification probe-Job cleanup authoritative: a successful probe can no longer report PASS when UID/resourceVersion-fenced Job deletion fails, times out, or observes a replacement; cleanup failure now emits explicit `probe-job-cleanup` FAIL evidence and makes the verification result fail closed;
- make TARGET_RUNTIME Network/Tenant-Isolation cleanup authoritative instead of discarding deferred cleanup errors: failure to remove any exact tracked Namespace, NetworkPolicy or probe Pod now appends `target-runtime/network-cleanup` failure evidence and prevents certification success;
- make TARGET_RUNTIME Storage/Backup cleanup authoritative for tracked PVC, VolumeSnapshot, probe Pod/ConfigMap and Velero Backup/Restore resources; a cleanup conflict/API failure can no longer leave side effects while the certification is recorded successful;
- update the stable Runtime Verification owner fixture to model Kubernetes deletion disappearance and add a negative control proving a completed probe with `409 Conflict` during cleanup is rejected; keep crash-orphan garbage collection explicitly open because safe post-crash deletion still requires durable pre-crash UID authority rather than name/label-only cleanup.

## 0.0.100 — Runtime Probe Termination Evidence & RBAC Closure

- remove the real-cluster `pods/log` permission mismatch from TARGET_RUNTIME network certification without widening the Agent ClusterRole: the digest-pinned `platform-probe` now writes its compact JSON result to Kubernetes' termination-message file, and Network/Tenant Isolation certification reads the authoritative result from `Pod.status.containerStatuses[].state.terminated.message` instead of the log subresource;
- move Runtime Verification probe-result collection to the same Pod termination-status contract, preserving Job-UID ownership selection while eliminating the baseline `pods/log` Role permission entirely;
- add probe termination-message encoding coverage, current-Job-UID status-selection regressions, network deny-proof fixtures based on real termination status, and a generated-manifest regression that forbids reintroducing `pods/log` access;
- keep prior-attempt TARGET_RUNTIME orphan cleanup explicitly open: automatic deletion based only on deterministic name/label was rejected because the exact pre-crash Kubernetes UID is not durable, so this release does not introduce an unsafe garbage collector or claim that crash-orphan cleanup is solved.

## 0.0.99 — Runtime Certification Resource Identity & Contract Closure

- align the real Agent Foundation INSTALL result with the Control Plane certification contract by emitting an authoritative `apply/Namespace/...` check for the separately created fresh namespace; add a cross-layer regression that passes the actual Agent-generated result directly through `ValidateRuntimeCertificationResultShape` so synthetic smoke fixtures can no longer hide apply-count drift;
- make Foundation VERIFY compare the live Kubernetes objects against all rendered desired fields instead of certifying existence/name alone, so drifted ResourceQuota, NetworkPolicy, ConfigMap or other managed state produces explicit failed verification evidence; make JSON numeric comparison semantic across in-memory integers and Kubernetes JSON float decoding;
- replace blind force-SSA for Runtime Certification network/storage/backup ephemeral resources with create-or-verify ownership semantics, bind cleanup to the exact `platform.4so.io/runtime-certification` owner and Kubernetes UID, and delete using fresh UID+resourceVersion preconditions so concurrent foreign/replacement resources are never adopted or removed by stale cleanup;
- fence Runtime Verification probe Job pre-cleanup and deferred cleanup to the verification ownership label and exact Job UID/resourceVersion, preventing a stale attempt from deleting a foreign or replacement Job with the same deterministic name;
- fence Tenant Suspend Pod deletion to the exact controller-managed Pod UID after a live ownership re-read and use UID+resourceVersion delete preconditions, so a same-name replacement or loss of controller ownership fails closed rather than being deleted;
- add negative controls for concurrent foreign certification objects, replacement cleanup, foreign/replacement Runtime Verification Jobs, same-name Tenant Pod replacement, real Agent INSTALL contract parity and post-install Foundation drift, without adding release-numbered validators or broadening product scope.

## 0.0.98 — Baseline Plan/Apply/Rollback ResourceVersion Fencing Closure

- bind Baseline planning to the actual live managed fields instead of trusting the `platform.4so.io/desired-digest` annotation alone, so a stale annotation can no longer hide post-deployment drift and produce a false `NOOP`; tolerate unrelated server/default fields while requiring every desired field to match;
- bind approved Baseline plan rollback evidence to the observed Kubernetes UID and normalized object digest for `NOOP`/`UPDATE`, then make APPLY re-read that exact identity/state before mutation: planned ADD uses create-or-verify, planned UPDATE rejects replacement or unapproved post-plan drift, and mutations use the current `resourceVersion` as an optimistic-concurrency precondition rather than blind force-SSA;
- fence `RESTORE_PREIMAGE` rollback to the exact object UID captured during PLAN and restore through conditional `PUT` with the current `resourceVersion`; same-name replacement or a concurrent mutation now fails closed with conflict instead of being overwritten, while an already-restored pre-image remains a safe no-op for resumed rollback;
- close the remaining existing-object ownership TOCTOU in Tenant Namespace/Velero Schedule reconciliation, Provider Cluster APPLY and Runtime Certification namespace retry by using conditional server-side apply after ownership verification; a replacement between GET and PATCH can no longer be force-adopted;
- add negative controls for stale desired-digest drift, foreign concurrent Baseline ADD, post-plan drift, resourceVersion conflicts and conditional owned-resource mutation, and update the stable Plan Safety smoke to carry the new PLAN-bound rollback identity evidence without weakening validators or adding release-numbered gates.

## 0.0.97 — Maintenance Node UID, Baseline Rollback & Certification Ownership Closure

- bind every Cluster Maintenance run durably to the Kubernetes Node UIDs observed in the exact requested inventory; approval and claim re-verify the same name→UID identities, the Agent verifies the live UID before cordon/drain, and cordon/uncordon use an atomic JSON Patch UID test so same-name replacement cannot receive an approved maintenance action; persist the binding in PostgreSQL migration 0041 rather than deriving identity after task claim;
- close the destructive Baseline rollback replacement race: `DELETE_CREATED_RESOURCE` now re-reads the live object, proves the baseline deployment/desired-digest ownership annotations, captures its Kubernetes UID and deletes with `DeleteOptions.preconditions.uid`, so a foreign or same-name replacement is never removed by stale rollback intent;
- close the Runtime Certification fresh-namespace create race by replacing absent-namespace force-SSA with create-or-verify semantics: `POST` the Namespace, re-read on `409 Conflict`, and reject a concurrent foreign namespace without any `force=true` patch; interrupted retries may reconcile only a namespace that proves ownership by the same catalog release/component;
- make the Maintenance node identity part of the durable Run/API contract, fail old/incomplete unfinished runs closed when no approved UID map exists, add negative controls for same-digest node replacement, missing UID inventories, foreign certification namespace creation and UID-fenced rollback deletion, and extend the stable Maintenance smoke to prove persisted/task `nodeUids` without adding release-numbered validators.

## 0.0.96 — PostgreSQL HA Migration Serialization & Startup Budget Closure

- serialize schema bootstrap and every migration decision across concurrently starting `platform-api` replicas with PostgreSQL transaction-scoped advisory locking; each waiter re-reads `schema_migrations` only after acquiring the lock, eliminating duplicate DDL/record races during three-replica HA startup;
- keep the migration lock crash-safe by using `pg_advisory_xact_lock` inside each schema/migration transaction rather than a session lock that could be stranded in a pooled connection;
- separate the 10-second PostgreSQL connectivity probe from schema migration execution so migrations and replicas legitimately waiting on the HA migration lock receive an independent 10-minute startup budget instead of inheriting the ping deadline;
- preserve per-migration transactional rollback and checksum-drift fail-closed behavior, and extend the stable PostgreSQL owner tests to prove advisory-lock ordering before schema creation/check/apply without adding release-numbered validators.

## 0.0.95 — Agent Resource UID Fencing & Provider Ownership Closure

- make Tenant Suspend classify a Pod as durably controller-managed only when an ownerReference explicitly carries `controller=true`; a mere owner reference no longer authorizes destructive suspension, preventing Pods with non-controller owners from being deleted under a false resume guarantee;
- close the Provider Cluster APPLY ownership boundary in the shared `4so-provider-system` namespace: existing Clusters are ownership-verified before SSA, missing Clusters are created with `POST`, and `409 Conflict` is re-read and rejected when a concurrent foreign Cluster took the name instead of being force-adopted;
- fence ownership-sensitive Tenant Namespace, Tenant Velero Schedule, legacy Schedule and Provider Cluster deletion with Kubernetes `DeleteOptions.preconditions.uid`, so a same-name resource replacement between ownership read and DELETE returns conflict instead of deleting the replacement object;
- add negative-control coverage for non-controller Pod owners, concurrent foreign Provider Cluster creation and destructive same-name replacement races while keeping the existing owner suites and validator structure stable;
- correct release SBOM annotation semantics so optional integrations such as Open Cluster Management are described as present only when explicitly bundled instead of being claimed as unconditional appliance contents.

## 0.0.94 — Tenant Atomic Resource Ownership & Legacy Cleanup Closure

- close the Tenant provision TOCTOU boundary for cluster-scoped Namespace and shared Velero Schedule resources: missing protected resources are now created with Kubernetes `POST`, `409 Conflict` is re-read and ownership-verified, and no SSA `force=true` mutation is allowed against a concurrently created foreign resource;
- preserve normal reconciliation for already-owned Namespace/Schedule resources while keeping the remaining tenant-owned resources on the existing server-side-apply path;
- close the direct-delete upgrade edge case for pre-runtime-contract Tenant states by deleting an owned legacy `tenant-platform-backup` Schedule even when the Tenant never passed through ACTIVE reconcile after upgrade;
- strengthen legacy backup ownership before destructive cleanup: both 4SO tenant ownership labels and the Velero `includedNamespaces` target must identify the same Tenant, otherwise deletion fails closed as ambiguous;
- add negative-control coverage for concurrent foreign Namespace/Schedule creation and for legacy Schedule cleanup/ambiguity without introducing release-numbered validators.

## 0.0.93 — Tenant Lifecycle Runtime Contract Closure

- align the Control Plane evidence contract with the real Agent Suspend workflow: `SUSPEND` now requires the eight normal Tenant policy artifacts plus authoritative `suspension/no-running-pods` evidence, so a successful Agent suspend report is no longer rejected after workloads have already been stopped; extend the stable Tenant owner smoke through Suspend → Resume using the real nine/eight-artifact contracts;
- make protected Tenant deletion cover the full product-owned runtime footprint: verify ownership of both the Tenant Namespace and its deterministic Velero Schedule before any destructive mutation, delete the shared-namespace Schedule first, then delete/observe the Namespace, and report `Deleted=true` only after both are absent; wrong-owner Schedule/Namespace objects fail closed without mutation;
- add durable Tenant runtime resource-contract versioning and PostgreSQL migration 0040 so ACTIVE Tenants loaded from older releases are automatically reconciled exactly once through the normal fenced Agent PROVISION path; after the new per-Tenant Schedule is applied and verified, an owned legacy global `tenant-platform-backup` Schedule is removed without touching a legacy Schedule owned by another Tenant;
- add upgrade, deletion, ownership and real-Agent evidence regressions in the existing owner suites and keep the change fail-closed without release-numbered validators or one-shot migration scripts.

## 0.0.92 — Tenant Backup Identity, RBAC and Namespace Ownership Closure

- make every Tenant Velero Schedule identity tenant-unique instead of reusing the global `tenant-platform-backup` name in the shared `velero` namespace, preventing one tenant from force-applying over another tenant's backup schedule; bind completion evidence to the exact deterministic per-tenant Schedule identity;
- fail closed before PROVISION/RESIZE/SUSPEND/RESUME mutation when a target Namespace already exists but is not labeled as managed by the same Tenant, preventing server-side apply with `force=true` from taking ownership of an unrelated or stale tenant Namespace; allow PROVISION to resume only when an existing Namespace proves the same Tenant ownership;
- align generated Agent RBAC with real runtime mutations by granting the previously missing `serviceaccounts` and Velero `schedules` permissions required by Runtime Certification and Tenant backup policy execution, with manifest regression coverage so fake Kubernetes smoke cannot hide a real-cluster `403 Forbidden`;
- add owner regressions for tenant-unique backup identity, API task identity, wrong-owner Namespace preflight and generated RBAC, while keeping the broader Agent credential blast-radius hardening as a separate non-functional follow-up rather than weakening current runtime workflows.

## 0.0.91 — Optional Fleet Hub and Installer Capability Closure

- Removed Open Cluster Management from the mandatory appliance bootstrap boundary: OCM is now included, air-gap indexed, deployed, and verified only when an explicit digest-locked OCM manifest is present in the bundle.
- Preserved optional OCM integration without making Factory installation depend on an unused fleet-management subsystem.
- Removed the planning-only `existing-kubernetes` option from the executable installer UI so users are no longer sent into a guaranteed planner rejection; API/planner semantics remain available for future target adapters.
- Added regression coverage for OCM-free bundle admission/bootstrap step selection and executable-installer capability exposure.

## 0.0.90 — Unified Agent Execution Fencing and Tenant Suspension Closure

- extend durable Agent task ownership to Baseline Deployment, Runtime Verification and Runtime Certification with monotonic fence tokens and active leases, closing the remaining immediate/multi-Agent double-claim paths left outside the 0.0.89 Tenant/Provider fencing contract;
- make Baseline planning respect active leases, fence stale reports after recovery, and fail an abandoned destructive rollback into explicit owner recovery instead of replaying rollback side effects after lease expiry;
- bind Agent-side Baseline, Runtime Verification, Runtime Certification, Tenant and Provider execution contexts to the authoritative lease deadline so an old worker cannot intentionally continue Kubernetes mutation after its execution lease expires;
- make Tenant Suspend a runtime-observed lifecycle operation: reject standalone Pods before mutation, establish the `pods=0` admission barrier, delete controller-managed Pods, verify zero remaining Pods from the Kubernetes API, and emit authoritative suspension evidence before committing `SUSPENDED`;
- add PostgreSQL migration 0039 and keep Memory/PostgreSQL/API/Agent schemas aligned for the newly fenced task families, including phase-to-phase lease release for Runtime Certification;
- add concurrent two-claimant regressions, stale-fence recovery tests, destructive rollback lease-expiry coverage, Agent lease-deadline coverage and Tenant suspension behavior tests; update HTTP/UI smoke consumers to report the claimed fence token without weakening validation.

## 0.0.89 — Agent Task Execution Lease Fencing Closure

- add durable lease ownership and monotonic task fence tokens to Tenant, Provider Profile and Provider Cluster agent tasks so active work is never immediately reissued to a second agent;
- reject stale or unfenced task reports and allow bounded recovery of idempotent non-destructive work only after lease expiry with a strictly newer fence token;
- fail abandoned destructive DELETE work into its owner recovery authority instead of replaying a potentially duplicated destructive side effect;
- persist the fencing contract in PostgreSQL migration 0038 and align Memory/PostgreSQL/API/Agent task schemas and behavior;
- make the fleet Agent Deployment use `Recreate` as defense in depth against rollout overlap, add a true two-caller ownership regression, exclude `.pytest_cache` from release artifacts, and serialize extracted-artifact race packages so the full verifier cannot deadlock under cross-package resource contention.

## 0.0.88 — Agent Task Recovery and Maintenance Fencing Closure

- make Cluster Maintenance ownership fail-safe across long drains and lost workers: advertise an explicit fenced-report Agent capability, size the lease from the requested node/drain workload instead of a fixed two minutes, reject stale fence/expired-lease reports, and transition abandoned destructive maintenance to `NEEDS_OPERATOR` rather than silently retrying or leaving it `RUNNING` forever;
- align PostgreSQL and in-memory Cluster Maintenance semantics so expired/cancelled windows, inventory drift and stale running leases commit their terminal transitions even when no task is returned, eliminating queued/running operations that could otherwise remain permanently stuck;
- make read-only Provider Profile verification recover from a lost report by reissuing `VERIFYING` tasks with a new attempt/revision and fencing reports from the stale attempt;
- make declarative Provider Cluster APPLY and Tenant provision/suspend/resume/resize recover from Agent restart or lost reports by reissuing in-flight states with incremented attempts/revisions, while retaining conflict rejection for stale reports and the existing observed-delete safety contract for destructive tenant deletion;
- update the Agent inventory/runtime and Cluster Maintenance smoke contract to exercise the fenced-report capability and operation fence token without weakening recovery/inventory-drift gates.

## 0.0.87 — Runtime Evidence Attempt and Lifecycle Identity Closure

- bind Runtime Verification probe evidence to the exact Kubernetes Job UID created for the current attempt, so a Pod left terminating from an earlier same-name Job cannot supply stale logs to a later verification;
- make network/tenant-isolation negative certification authoritative: a generic Failed Pod no longer proves NetworkPolicy denial, and the negative path must return structured probe evidence showing DNS succeeded, TCP did not connect, and the failure was the expected TCP connection failure;
- scope Loki and Alertmanager observability evidence to the current runtime-certification execution attempt, verify the exact pushed probe line rather than merely any matching stream, and reject stale prior-attempt logs/alerts after report-failed retries;
- make Lifecycle run and producer backup identifiers collision-safe when the wall clock repeats or moves backward, use the same uniqueness rule for legacy recovered runs missing a backup ID, and fail closed when durable lifecycle state already contains ambiguous duplicate run/producer-backup identities.

## 0.0.86 — Kubernetes Deletion Observation and Certification Retry Closure

- make tenant destructive deletion authoritative instead of optimistic: the Agent observes Namespace disappearance, the Control Plane keeps the tenant/destructive operation `DELETING/RUNNING` until that proof arrives, and rolling upgrades fail closed by advertising/claiming the new behavior through the `tenant-delete-observed` inventory capability rather than assuming an Agent version;
- make Kubernetes object replacement wait for deletion completion before recreating deterministic Runtime Verification Jobs, eliminating retry/report-failure races with objects still `Terminating`;
- make Baseline rollback observe deletion of forward-created resources before reporting rollback success, so DELETE acceptance is no longer mistaken for completed rollback;
- strengthen target Runtime Certification backup/restore proof by observing the backup probe ConfigMap as absent before Velero Restore begins, eliminating a false PASS where Velero could skip an object that had not actually disappeared;
- isolate Runtime Certification retry resources by task attempt after the first attempt so PVC/Snapshot/Velero/Network probes from a previous report-failed attempt cannot collide with asynchronously terminating objects on retry;
- update destructive smoke fixtures to advertise the actual observed-delete capability and report explicit deletion evidence, preserving stale-recovery and owner-operation gates without weakening product semantics.

## 0.0.85 — Runtime Contract, Crash Consistency and Resume Closure

- align executable installation planning with the bootstrap runner that actually ships: unsupported external Git/registry/database/identity adapters and unimplemented ACME/external-certificate modes now fail closed, provider/credential/TLS/DNS/storage contracts are validated at planning time, HA management nodes must be unique, and the planner's normalized effective request is the exact request persisted and executed;
- make durable bootstrap resume truthful after real host mutation: do not re-run fresh-host preflight once its accepted step is durable, revalidate the current executable contract, SpecDigest and BundleDigest before any resumed side effect, prevent PENDING/RUNNING/FAILED runs from being overwritten by a new Start, fail startup on unreadable journal state, and keep Status responsive while long-running execution owns only the execution guard rather than the status mutex;
- introduce a shared fail-closed durable-file replacement primitive with file sync, atomic rename and parent-directory sync, and apply it to bootstrap side effects/journals, control-plane state, lifecycle/DR state and job manifests, installer-access state, agent credential cache, field-campaign state and platformctl private state so a durable journal cannot outrun the filesystem mutation it records;
- harden lifecycle and DR crash boundaries: fsync service backup payloads before hashing and committing metadata, make DR backup/run identifiers collision-safe even under identical timestamps, refresh agent credentials from the authoritative Kubernetes Secret before trusting a local cache, and preserve fail-closed mutation exclusivity across interrupted bootstrap/lifecycle/DR operations;
- make external Catalog runtime bundle installation crash-repairable and immutable: write and sync a complete staging tree before atomic publication, repair only partial directories whose existing files exactly match verified evidence, and reject any conflicting or unexpected content;
- make Host Deployment and Remote Bootstrap directory swaps durable and recoverable: sync parent directories around bundle/stage renames, keep the previous remote stage until the new stage is durably published, recover an interrupted swap on retry, and use durable private-file writes instead of silently ignoring directory-sync failures;
- remove an Installer smoke race that could mistake the previous terminal bootstrap run for a newly accepted asynchronous run; assurance now waits for a new run identity and expected profile rather than changing correct product timing.

## 0.0.84 — DR Integrity, Encryption and Signing Authority Closure

- bind every whole-appliance DR payload to a SHA-256 sidecar, verify all component objects before quiesce/destructive restore, and re-verify each payload immediately before its component mutation so truncated/corrupt backups fail closed;
- encrypt PostgreSQL, Forgejo and zot backup payloads and all checksum sidecars with the same S3 SSE-AES256 contract already used for platform authority data, eliminating mixed encrypted/unencrypted backup sets;
- version the durable backup format and reset legacy interrupted backup progress before resume, preventing a pre-integrity partial run from skipping old components and later writing a misleading current-format completion marker;
- preserve the installer-local GitOps Ed25519 signing authority across whole-appliance DR without making Kubernetes a new long-term source of truth: backup reads the private key from the verified Manager node, restore mirrors it back to installer-local state, then removes the transient Kubernetes copy;
- persist the Manager authority backup node before side effects and add owner regressions for digest ordering, shell syntax, encryption, legacy-resume migration, GitOps key continuity, Manager-node pinning and transient-secret cleanup.

## 0.0.83 — DR Runtime Authority, Lifecycle HA and Mutation Correctness Closure

- extend whole-appliance DR with off-node backup/restore of runtime authority secrets required to preserve Forgejo admin identity, API session/catalog signing authority and ingress TLS/CA, repair the Argo CD internal-Git credential after restore, and mirror restored authority back into installer-local state without reintroducing the revoked bootstrap credential;
- add a profile-bound completion marker and restore preflight covering every whole-appliance component so incomplete/legacy backup sets fail before quiesce or destructive restore, and make DR/Lifecycle Job replacement synchronously remove any prior immutable Job before apply;
- make service Lifecycle profile-aware and durable: HA Forgejo/Keycloak backups use their actual database-owner credentials, logical restores no longer require database create/drop authority, backup/restore quiesce and observe writer termination, failed restores remain quiesced, and HA replica counts are restored correctly;
- pin Lifecycle hostPath maintenance Jobs to the verified installer/RKE2 execution node and run filesystem maintenance with the authority required to preserve PVC ownership while privilege escalation remains disabled;
- serialize bootstrap, service Lifecycle and whole-appliance DR mutation domains across HTTP admission and restart/reconcile state, while preserving the explicit Resume path for an interrupted durable bootstrap run; add owner regressions for shell syntax, secret recreation, HA credentials, quiesce barriers, Job cleanup, durable profile/node identity and mutation overlap.

## 0.0.82 — Disaster-Recovery PostgreSQL, Shell and Filesystem Correctness Closure

- replace HA `pg_dumpall`/single-app-role whole-cluster backup with three owner-scoped logical database dumps for `platform_factory`, `keycloak` and `forgejo`, using the actual CNPG-managed credentials for each database and retaining a single-node legacy restore fallback while rejecting unsafe legacy HA restores;
- make PostgreSQL dump/restore failure propagation explicit by removing left-side `pg_dump | gzip` and `gunzip | psql` pipelines whose failures could be hidden without `pipefail`; every dump, decompression and `psql -v ON_ERROR_STOP=1` step is now sequenced with durable `&&` boundaries;
- isolate S3 object keys in a quoted environment variable instead of interpolating ObjectStorage prefix data into shell programs, and strictly validate/YAML-quote the external Secret name as a Kubernetes DNS-1123 subdomain;
- run DR maintenance jobs with the filesystem authority required to read/preserve Forgejo/zot PVC ownership and the host RKE2 kubeconfig used by Agent-PKI restore, while disabling privilege escalation;
- add owner-suite regressions covering HA/single-node PostgreSQL credential contracts, legacy restore behavior, shell-injection boundaries, hidden pipeline failures, strict credential references and maintenance filesystem authority.

## 0.0.81 — Disaster-Recovery Quiesce Barrier and Durable Profile Closure

- make whole-appliance backup/restore wait until every API, Keycloak, Forgejo and zot writer pod has actually terminated after scale-to-zero before any backup or restore job may begin;
- fail the quiesce boundary closed if writer shutdown cannot be observed, and recover every workload already scaled down when that pre-side-effect barrier fails;
- persist the installation profile with every DR run and make it authoritative during restart/reconcile, preventing an HA operation from silently resuming with single-node replica counts when current installer status is unavailable or has drifted;
- reject DR execution when neither durable nor migration-fallback profile identity is available, and add owner-suite regressions for shutdown ordering, observation failure recovery and durable profile migration.
- replace the live Catalog trust smoke's fixed 250 ms timing assumption with a condition-based UI wait so assurance follows real async API completion instead of producing intermittent false failures.
- align full extracted-artifact verification with the canonical smoke environment, preventing false 401 failures while leaving production authentication defaults fail-closed.

## 0.0.80 — Disaster-Recovery Consistency Quiesce Closure

- quiesce platform API, Keycloak, Forgejo and zot writers before whole-appliance backup/restore so PostgreSQL, repository and registry state are not captured or rewritten while authoritative writers remain active;
- persist the pre-quiesce Forgejo/zot node placement before side effects and pin RWO backup jobs to those nodes, preserving restart/reconcile safety without depending on a live service pod;
- restore profile-correct replica counts after backup, keep workloads intentionally quiesced after a failed restore to avoid serving mixed state, and recover already-stopped workloads if quiesce itself fails before backup/restore begins;
- add owner-suite regression coverage for HA/single-node replica restoration, RWO placement persistence, backup failure recovery and fail-closed restore behavior.

## 0.0.79 — OIDC Login Entropy Fail-Closed Closure

- fail closed before OIDC redirect when cryptographic entropy for state, nonce, or PKCE verifier generation is unavailable;
- fail closed if OIDC state/session signing cannot be completed instead of emitting an unusable authentication cookie;
- add owner-suite regression coverage proving entropy failure creates neither a redirect nor authentication cookies.

## 0.0.78 — Security, HA and release correctness closure

- Fail-closed production authentication boundaries and bootstrap credential scope.
- HA installer credential/storage correctness and air-gap/runtime checks.
- OCI/release identity, non-mutating smoke evidence and truthful provenance/SBOM.
- Fail-closed authoritative API/agent error handling and Codex Autopilot coverage closure.

Only the canonical release history is kept in this file. Older repository history remains available from Git.
