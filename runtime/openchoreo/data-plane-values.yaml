# 4SO optional target profile: do not install a duplicate monitoring stack.
# The target capability resolver decides native networking/ingress ownership.
kube-prometheus-stack:
  enabled: false
clusterAgent:
  metrics:
    serviceMonitor:
      enabled: false

# Keep the OpenChoreo data plane internal; distribution/target ingress remains authoritative.
gateway:
  enabled: false
